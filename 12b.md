# Visby — QA Fix Cycle 12b (Phase 12 · batch B)

> Judah's manual QA pass (website + mobile, 2026-07-08). This is the working fix list.
> **Part 1 loaded 2026-07-08 · Part 2 loaded 2026-07-09 — the full pass is now in this file.**
> Full QA scorecard: 60 pass / 25 fail / 10 partial / 1 n/a / 13 untested of 109 items.

> ## 🚀 DEPLOYED 2026-07-15 — rounds 3+4 + SDK rebuild all pushed to main (commit `a2f569f`); Vercel building
> Includes: SDK-MINT fix (condition + delegate transfer), SDK-PAY (primary-method default + Moov card rail via /api/sdk/moov-charge, Stripe fallback when flag off), SDK-UX (3-currency confirm sheet), SDK-NAV (nav+footer stripped), `sdk_orders.moov_transfer_id` migration APPLIED LIVE. Verified pre-push: tsc 0 errors, home renders, changed routes 200, SSR nav-strip confirmed. Next: 8-step live SDK test with Judah in the internal browser.

> ## ROUND 4 COMPLETE 2026-07-13 — EVERY open item built/fixed; awaiting DEPLOY + Judah retest
> **State:** `tsc` 0 errors · 231/231 vitest green · all pages 200 + home browser-verified. 21 subagents + 1 workflow across two waves (see ROUND 4 fix-log entries). Migrations LIVE on prod: cart_items, business_verifications (+self_ship), sdk_demo_config, moov_cards. **PUSHED c7a9306 → main 2026-07-13 (Judah's conditional go after my browser sweep) — Vercel deploying; retest on visby.me once live.**
> **Left needing Judah:** deploy go-ahead · Moov flag flip after one sandbox charge ("flip Moov") · Coinbase Commerce account + `COINBASE_COMMERCE_API_KEY`/`COINBASE_COMMERCE_WEBHOOK_SECRET` when ready (B2b is flag-dark) · W3 = 30-sec Phantom reject-a-signature test (no wallet extension in my sandbox) · fill Moov's "expected activity" KYB prompt before PRODUCTION cards.
> **Stashes stash@{0}/stash@{1} from the mid-wave git incident: everything recovered — safe to drop after retest passes.**

## How this loop works (flashcards)
1. Every item below has a stable **ID** and a **status**.
2. Claude fixes items, marks them 🟢 **fixed — awaiting retest**, and writes exactly what changed in the **Fix log**.
3. Judah retests. Working → ✅ **done** (checked, drops out of the active rotation, kept as a record). Not working → back to 🔴/🟡 with a note, and it **recycles** until it passes.
4. We keep cycling until a full clean run, then the batch closes.

## Status legend
- 🔴 not started
- 🟡 in progress (being fixed now)
- 🟢 fixed — awaiting Judah retest
- ✅ done — Judah confirmed working
- ⚠️ **blocked on Judah** (dashboard/config/keys — code can't fix alone)
- 🅿️ big lift — staged (architectural; broken into sub-steps below it)

## Triage (how the work is ordered)
**Waves 1–2 — SHIPPED 2026-07-08, 🟢 awaiting Judah retest** (see Fix log + Retest queue below).
**Wave 3 — quick/isolated Part-2 items:** S4 (selected-button borders), ERR1 (error-message sweep), D1 (ratings blank until rated), N2 (badge alignment), QR1 (remove Tally QR), MOB1 (mobile drag), PERF1 (button reactivity), RATE1 (login rate-limit), SES1 (session/device-trust model).
**Wave 4 — money-path & careful:** B1 (Moov default card rail + saved-card auto-use), B2a (fix SOL/USDC crypto rail), B3 (escrow + devnet transfer + pay-request popup), SH1+N1 (label UI + sold/payout notification pipeline), SH3/SH4/SH5 (delivery-confirm/ratings/address UI).
**Wave 5 — big architectural lifts:** A7/P1 (business account + verification, settings-based, SDK auto-business), SU1 (sign-up overhaul), CART1 (persistent cart), W2 (omni-currency), B4 (Apple-Pay-style checkout), A8 (list-page gate + preferred-currency pricing), B2b (Coinbase Commerce), DES1 (full design sweep).
**Claude self-checks (no Judah needed):** W3 (signature-reject), SEC1 (security spot checks), L3 (bulk-serial test setup), DES1 (preview sweep).
**⚠️ Blocked on Judah config (I'll flag + prep, you flip):** A2 (social login = Privy providers), A4 (embedded wallet = Privy Solana toggle), A5/A6 (reset email — config verified fine, root = A4), B1 (Moov capabilities confirm → `NEXT_PUBLIC_MOOV_ENABLED=1`).

## Decisions locked 2026-07-09 (Judah)
- **B2 crypto = BOTH**: fix the existing SOL/USDC/Li.Fi rail now (**B2a**); build Coinbase Commerce as a later wave for max compatibility (**B2b** — unparked from blueprint).
- **Session model (SES1)**: stays signed in indefinitely; **1 hour of inactivity → Face ID/passkey re-entry** on a recognized device; full email→password→2FA ONLY on an unrecognized device or after explicit sign-out. Refresh must never sign you out (mid-payment safe).
- **Cart (CART1) = Amazon-style**: cart data lives on the ACCOUNT (server-side), survives refresh/devices, empties only on purchase or manual removal. The one-click "click and confirm" buy button stays a SEPARATE path.

---

# PART 1

## Authentication & Onboarding
- 🟢 **A1** — Sign-in must force a password on **every** method; split the entry into two clear actions: **Sign in** and **Create account** (creating from login still allowed — just make it feel like two options).
- 🟡 **A2** — Add **social login (Google / Apple)**. **UNBLOCKED 2026-07-09 — Judah enabled Google + Apple OAuth in the Privy dashboard.** Now a code task: add `loginMethods:['google','apple',...]` + the branded buttons on the login/create-account screens (folds into SU1).
- 🟢 **A3** — External-wallet (Phantom/Solflare) sign-up works but has **too many steps** and exposes a code/command mid-flow. Reduce to the fewest clicks from "Sign in" → done; hide the raw code/command.
- ⚠️ **A4** — Email users' **embedded Solana wallet isn't auto-creating** (new account got no wallet). *(Prime suspect: Privy dashboard "Solana embedded wallets" toggle — the known silent-createWallet hang. Verify code path too.)*
- 🔴/⚠️ **A5** — Wrong password / bad code should show a **clean error, not a raw dump**, AND the **password-reset code isn't sending**. *(Reset email likely blocked on Resend domain verification; also audit the reset-request route.)*
- ⚠️ **A6** — **Password reset / recovery** end-to-end doesn't work. *(Same Resend dependency as A5 + confirm the confirm-route.)*
- 🅿️ **A7** — **Business vs. personal**: move the business switch **out of the sell page into Settings**, behind a **full business-verification flow** (EIN, business info, proof — legality/security), and let business accounts **toggle off Visby shipping and self-ship**.
- 🔴 **A8** *(partial)* — Civic gate blocks listing but **doesn't say why** → block the whole list page for unverified users with a reason. **One account per person** (no re-verification loop). On the listing page, show the price in the user's **preferred currency (translated), not just USDC**, and replace any **black-outlined elements** with the site's white-bubble-shadow style.

## Wallets & Linking
- 🔴/⚠️ **W1** — **Linking a second external wallet is broken** and should be more than a destination address — it should be a **payment method too** (redirect into the wallet-linking flow). Remove the "coming soon."
- 🔴 **A9** *(new 2026-07-13)* — **Wallet linking doesn't actually link** and still forces an email sign-in. A connected wallet should be **sufficient to sign in on its own** (Privy wallet-only login). Fix linking + wallet-as-sole-login. Pairs with W1.
- 🔴 **ADM1** *(new 2026-07-13)* — **Admin entry too exposed.** Replace the prominent admin block with a tiny **"admin"** text link at the **bottom of the Settings page** (next to privacy/legal links) — clickable, low-key.
- 🅿️ **W2** *(partial)* — **Omni-currency**: support ~**100 currencies (crypto + fiat)**, any settable as **preferred**, users **auto-receive in their preferred currency**. Today only ~8 and it's clunky. Add a **sign-up info popup** explaining the omni-currency system. *(Copy-address button: noted not necessary.)*
- 🔴 **W2b** — **Gradient coverage sweep**: some gradients clip / don't cover the full shape. Do a site-wide once-over and fix every clipped gradient.
- 🟡 **W3** *(partial)* — "Reject a signature cancels cleanly" — Judah can't test. Claude to attempt via browser/Composio if possible (creds provided out-of-band), else skip silently.

## Marketplace — Browsing & Search
- 🟢 **S1** — **Empty search returns everything** (should be graceful/empty). Also **tolerance too narrow** (search "succulent" → a cactus won't show). Improve relevance; AI/semantic engines welcome.
- 🟢 **S2** — **Counterfeit flag is inverted + wrong button**: remove the "authenticated" button entirely (we show provenance transparency, not authentication). The **flag** should be a **user** action that **notifies admins to review/take down** — currently admins flag and users see. Flip it.
- 🟢 **S3** — **Shared listing URL forces sign-in** for logged-out users → must open publicly. **Session: stay signed in ~1 hour** before re-auth.

## Listing Creation
- 🟢 **L1** — **Required-field validation is missing** — listed with **no photo**. Enforce all required fields (incl. at least one photo) before publish.
- 🟢 **L2** — **Edit a live listing**: currently price-only. Allow **adding photos** (add only, can't remove originals) and **editing the description**. **Title stays locked** (tied to serial).
- 🅿️ **L3** *(partial)* — **Bulk serial logging** (business) — Judah can't test. Claude to set up a testable path/walkthrough.
- 🔴 **L4** *(new 2026-07-09)* — **Listing-page display cleanup**: (a) move **"SN •••• · visible to owner"** onto the **Tally**, not the listing area; (b) show the **first 4 lines of the description by default** (expandable); (c) price shows the **seller's preferred currency**, never "≈ USDC".

## Purchases & Sales
- 🅿️ **B1** — **Card must run on Moov, never Stripe.** Card checkout silently falls back to Stripe's CardElement because Moov is gated off → no saved-default-card, re-typing card each time (the whole point of Moov). Stripe should only touch **bank/money-management**, never a card. *(Make Moov the default card rail; saved default card auto-selected; then flip `NEXT_PUBLIC_MOOV_ENABLED=1`.)*
  - **✅ SANDBOX READINESS CONFIRMED 2026-07-09 (Chrome-Claude checked Visby Inc test account):** the ONLY capability the online card rail touches — **Collect funds → Card payments = ENABLED** — plus **Wallet balance + Basic transfers = ENABLED**, and **`https://visby.me` is already on the API-key domain allowlist** (correctly scoped; webhook at `/api/moov/webhook` on root). `MOOV_WEBHOOK_SECRET` + all Moov creds already in Vercel. **So Moov IS ready for B1.**
  - **NOT blockers for B1 (ignore for this rail):** the ACTION-NEEDED items (Send funds → Instant bank / Push to card; Money transfer → Pull from card / Push to card) are OTHER flows we don't use in card checkout. **Card-present/in-person** isn't a distinct toggle — it's DEFERRED to Phase 9 (blueprint 4.6), so N/A here; online "Card payments" is what B1 uses. The **"Expected activity is missing information"** banner is a Moov underwriting/KYB prompt — harmless for a sandbox charge test; **Judah should fill it before PRODUCTION** (it gates live capabilities, not sandbox).
  - **PLAN:** build the B1 code first (Moov default rail + saved-card auto-use) → one sandbox charge test (4242-style) → flip `NEXT_PUBLIC_MOOV_ENABLED=1`.
- ❓ **B2** — "Buy with crypto (**Coinbase Commerce**)" failed. **Coinbase Commerce was never built** (parked). Real crypto path is SOL/USDC/Li.Fi. **Decision needed:** build Coinbase Commerce, or treat SOL as the crypto rail and fix that flow?
- 🅿️ **B3** — **Escrow untestable**: sales didn't go through, **devnet not actually transferring**. **Pay-request UX (clarified 2026-07-09):** build a **dedicated pay-request page** AND an **in-message preview** (accept/decline inside the message app) — **no small floating window on the pay page**. Get a devnet money transfer working end-to-end so escrow can be verified on-chain.
- 🅿️ **B4** *(expanded 2026-07-09)* — **Apple-Pay-style checkout preview**: big bold gradient price = **buyer's preferred currency**. If the spend source differs, small light-grey **"Using: 0.89 SOL"** (actual wallet/bank amount) + small light-grey **"Transferring to: 72 USD"** (seller's currency). Two–three currencies only — **never inject USDC** for a non-USD user. Auto-pay primary method, viewer screen to change. Model on Apple Pay.

## Shipping & Delivery
- 🅿️ **SH1** — **Label generation has no UI.** Build a **seller popup walkthrough**, plus a **notification-center button** (link back to that popup) with **order-progress info** so sellers know where their money stands.
- 🟢 **SH2** — **Tracking number** shows to the **buyer but not the seller** — show it to both.
- 🅿️ **SH3** — **Delivery confirmation → escrow release + ratings prompt**: no UI in-app. Build it.
- 🅿️ **SH4** — **Ratings**: submit / display on profile / can't-rate-twice — **no UI**. Build it.
- 🅿️ **SH5** — **Invalid / foreign address handling** — no UI. Build it.

## Profiles, Settings & Account
- 🅿️ **P1** — **Business account settings must be fully separate** from personal (no bleed-over) and easy to switch to. **SDK-page sign-up → auto-create a business account** and route through the business-verification steps. *(Pairs with A7.)*
- 🔴 **P2** *(partial)* — **Username doesn't display** anywhere; the **redundant name+description box** on the profile page must go (no redundancy). **Add the username where Instagram puts it**; clean up the profile page.
- 🔴 **P3** *(partial)* — **Notifications/settings polish**: (a) tapping a user's name doesn't always link to their page → it should; (b) an **offer** notification should say **which product** it's for and offer an **Accept** action; (c) **toggle switches look bad** — the white needs **inset/indented shadow**, and the gradient **clips** (doesn't cover the whole toggle); (d) **Payment Methods** shows "Crypto (SOL, USDC) — SOON" → should be **now** (make it work, or change the view to reflect reality).

## Polish (passing items, tester notes)
- 🟢 **POL1** — **Provenance owner count**: every transfer counts as an owner (1→2→1 = **3 previous owners**), and show the owner/purchase count on the selling thumbnail.
- 🟢 **POL2** — **Delete button** is black text on a black button → unreadable. Fix contrast (keep the rest of its look).
- 🟢 **POL3** — **Shipping address fill-in doesn't save to the address book** → make address entry + address book **one setting**.
- 🟢 **POL4** — **Remove the 2FA toggle** — 2FA is always-on and can't be disabled, so the setting is misleading. Remove it.
- 🔴 **POL5** — **REVERSED 2026-07-09**: do **NOT** nudge "passkey instead of a password" on sign-up. Remove that nudge. Face ID is app-lock only (see A1/SU1). Keep the existing Settings passkey control.
- 🟢 **POL6** — **Active sessions** lists **more devices than owned / duplicates** → dedupe to real sessions.
- ✅-note **POL7** — Declined-card test worked but ran on Stripe → folds into **B1** (must be Moov).

---

# PART 2 (loaded 2026-07-09)

> Part 2 of the same QA pass. Items that duplicate Part 1 keep their existing IDs (noted). New IDs below.

## Search & browse visuals
- 🔴 **S4** — Search/browse **selected buttons show black borders** — remove them; selection state = a softer/gradient shadow per the white-bubble pattern, never a hard border.

## Content & errors
- 🔴 **ERR1** — **Human-readable errors sweep**: no stack traces, no "undefined", no raw dumps anywhere. Audit every error surface (auth, checkout, forms, API failures) and map to plain-English messages.

## Dashboard & data
- 🔴 **D1** — Seller-dashboard **ratings show sales count instead of real ratings** — ratings must be **blank until a buyer actually rates**; never derived from sales.

## Item page
- 🔴 **QR1** — **Remove the Tally QR code** from the item page (later thing — pairs with the deferred NFC 6.7; blueprint 2.1 stays built, UI hidden for MVP).

## Notifications
- 🅿️ **N1** — **Sold/payout notification pipeline** (pairs with SH1): when an item sells → **push notification + popup on next app open + PINNED notification on the notifications page until shipped** (with the label-generation button + order-progress/money-status info). Same treatment for the **payout** landing. Checkout shipping info must be easy to reach.
- 🔴 **N2** — Notification **badge number not aligned with the dot** on the inbox icon — clean up.

## Sign-up & session
- 🅿️ **SU1** — **Sign-up overhaul**: agree to Terms & Privacy on sign-up; collect name, username pick, profile pic; walk through the explainer steps (incl. the W2 omni-currency popup); offer passkey/Face ID activation (extends POL5). Auth order: email → password → 2FA (Privy email); Face ID skips all of that on a recognized device.
- 🔴 **SES1** — **Session/device-trust model** (per 2026-07-09 decision): signed in indefinitely; 1h **inactivity** → Face ID re-entry; full email+password+2FA only on unrecognized device or after sign-out; **refresh never signs out** (mid-payment safe). Supersedes the wave-2 "~1h hard TTL" implementation — rework it.

## New feature
- 🅿️ **CART1** — **Cart, Amazon-style**: account-linked server-side cart (add/remove from listing pages), survives refresh + devices, empties only on purchase or removal; batch checkout for multiple items; keep the existing one-click "click and confirm" buy as a separate path.

## Performance & mobile
- 🔴 **PERF1** — **Button reactivity / loading times on all pages** — clicks feel a second delayed; add instant press feedback (optimistic UI/transitions) and profile slow loads.
- 🔴 **MOB1** — Wallet **payment-methods drag-to-reorder doesn't work on mobile** (works on desktop) — add touch drag support.

## Security & checks (Claude runs these)
- 🔴 **SEC1** — **Security spot checks via browser/Composio** (Judah delegated): API keys not visible in page source or network responses; sweep the other security checklist rows the same way. Report findings, fix what surfaces.
- 🔴 **RATE1** — **No limit on failed login attempts** — add rate limiting/lockout backoff (low priority given forced 2FA, but close it).

## Design
- 🔴 **DES1** — **Full design sweep with preview** vs design.md: gradient not reaching shape edges (site-wide, matches W2b), things not centered, any drift from spec. Nooks and crannies.

## SDK standalone test track (folded into the checklist 2026-07-09 per Judah)
> These are VERIFICATION checks (not bugs), run separately from the marketplace via `docs/sdk-test-instructions.pdf` (curl + Chrome-Claude prompts A–F). Track pass/fail alongside everything else.
- 🔴 **SDK1** — Create a merchant + capture the `sk_visby_` secret key (dashboard `/merchant`).
- 🔴 **SDK2** — Create a checkout session by API (`POST /api/sdk/checkout`, Bearer key) → returns hosted URL.
- 🔴 **SDK3** — Merchant page embed: `<visby-button>` renders + opens the checkout popup.
- 🔴 **SDK4** — Complete a purchase (Stripe test card) → `visby:complete` fires with order_id + nft_address.
- 🔴 **SDK5** — Webhook delivery lands (HMAC-signed) + manual "Re-send webhook" works.
- 🔴 **SDK6** — Merchant dashboard numbers: fee = 3.5% (min $0.50), merchant net = price − fee.
- 🔴 **SDK7** — The minted Tally is real on a devnet explorer + shows in buyer's My Items.
- 🔴 **SDK8** — Failure paths clean (401 bad key · 400 missing serial/price · 400 EUR · 422 rejected serial · 429 · declined card = no order/mint).
- 🔴 **SDK9** *(known gap, not an SDK bug)* — SDK-page signup should auto-create a **business account** (ties to P1/A7) — expected to fail today; do not fail the SDK for it.
- 🟢 **SDK-SBX** *(new 2026-07-13, BUILT + boots)* — **Burner merchant sandbox** to test the SDK standalone: `sdk-sandbox/server.mjs` (single-file Node, no deps) + `README.md`. Serves a test storefront (test items + auto-unique serials + a serial→session→order→NFT log) that creates REAL checkout sessions with your `sk_visby_` key and embeds the real `<visby-button>`. Run: `VISBY_SECRET_KEY=sk_visby_… node sdk-sandbox/server.mjs` → localhost:4000. Verified: boots + serves 200.
- 🟢 **SDKT1** *(new 2026-07-13, BUILT — awaiting verify+retest)* — **Install tutorial on the merchant page.** A "How to install" button in the merchant header opens an **opt-in, dismissible** 5-step guide (create account → server session → drop the button → handle `visby:complete` → verify webhooks with the real `Visby-Signature: t=…,v1=<HMAC-SHA256>` scheme) + a sandbox test-card note + link to the full `/sdk` guide. **Not** a forced popup — opens only on the header button. File: `src/app/merchant/page.tsx`.

### SDK test RUN — 2026-07-13 (Claude, headless + in-app browser, against LIVE visby.me)
> Seeded a dedicated **"SDK Test Shop"** merchant (owner_wallet `SDKTEST_HARNESS`, id `9148f4e2…`, wired to a webhook.site endpoint) via service-role script so Judah's real merchant is untouched.
- ✅ **SDK2** — session create returns `{session_id, checkout_url}` (HTTP 200).
- ✅ **SDK6** — fee math correct: $25 → fee_bps 350, platform_fee **$0.88** (3.5%, floor OK), merchant_net **$24.12**.
- ✅ **SDK8** — failure paths all CLEAN, human-readable, no stack traces: bad key→401 "Invalid API key"; missing serial→400; currency EUR→400 "Only USD is supported today"; negative price→400 "price must be a number greater than 0".
- ✅ **CARD-VS-ACCOUNT (Judah's concern) — RESOLVED in Judah's favor:** the hosted SDK checkout ENTRY screen is **account-first** — shows item + total + **"Create a Visby account" / "Log in"**, with **NO raw card field**. The "Stripe test card" wording was in my test PDF, not the SDK UI. (What still needs a login to confirm: the post-login PAYMENT step — does it use the buyer's primary method / Moov, or offer a raw card? = Judah's vital click.)
- 🔴 **SDK-NAV (new finding)** — the hosted checkout page `/sdk/checkout/[id]` renders **Visby's app bottom-nav** (Home/Inbox/Sell/Wallet/Profile) + legal footer. A merchant-embedded popup must NOT show Visby's internal marketplace chrome — strip the BottomNav (and app shell) from the `/sdk/checkout` route.
- ⏳ **SDK4/SDK5/SDK7** — need a completed payment (login + pay) → Judah's clicks; webhook.site is armed to catch the delivery. **Live test URL:** `https://visby.me/sdk/checkout/baf9521e-d9cc-4b66-b912-698f6a9a1fdf` · **watch webhooks:** `https://webhook.site/#!/b1b5f31e-f6db-4d06-85fc-0c040956492b`

### SDK test RUN #2 — 2026-07-13 (guided walkthrough on Judah's REAL merchant "test2")
> Ran the 8-step walkthrough live. Merchant "test2" (id `772dead7…`, owner `7E4NKe3S…`).
- ✅ **SDK1** — pulled a fresh secret via the dashboard **Rotate** (`sk_visby_…ddd1`, DB-verified hash match) + set **Webhook URL** to the webhook.site catcher (persisted to the row). Proves "create merchant → pull real keys + webhook."
- ✅ **Webhook pipe** — a manually-signed event (`whsec` HMAC over `t.body`) delivered to webhook.site **HTTP 200** with a valid `Visby-Signature`.
- ✅ **SDK2/SDK3** — session created with the real `…ddd1` key (200); hosted checkout renders in-browser showing **"Sold by test2"** + item + **$25.00** + account-first CTA.
- ✅ **SDK8** — re-run LIVE from the browser (fetch on visby.me): bad key 401, missing serial 400, EUR 400, negative price 400, valid control 200. All clean errors.
- ⏳ **SDK4/5/6/7** — genuinely gated on Judah's login + payment on the hosted checkout (buy → webhook lands on webhook.site → order+fee on `/merchant` → Tally mints). Cannot be automated (buyer auth + funded method).
- 🔴 **SDK-NAV (reconfirmed)** — hosted checkout still shows the app bottom-nav; strip app shell from `/sdk/checkout`.

### SDK REAL-PURCHASE test (Judah paid $25 on order SDKTEST-2921) — 2 CONFIRMED MONEY BUGS
> Judah completed a real checkout. Result: charged, but **no Tally delivered** + **wrong payment method**. Root-caused both.
- 🟢 **SDK-MINT (S0 money — buyer charged, no NFT) — ROOT-CAUSED + FIXED 2026-07-13.** NOT transient — **TWO deterministic bugs stacked**, so EVERY SDK purchase failed to deliver the Tally: **(1)** `sdk-mint.ts` inserted the `items` row **without `condition`**, but `items.condition` is `NOT NULL CHECK IN ('new','like_new','good','fair')` → `23502` every time → `status='failed'` before the transfer even ran. FIX: `condition:'new'` in the insert. **(2)** hidden behind #1: `transferFromAuthority` used `transferV1(umi,{asset:<pubkey>,newOwner})`, which only works when signer==owner (normal mints, owner=authority). SDK assets are owned by the **merchant** and moved by the authority as a **PermanentTransferDelegate** → that call **panics on-chain** (`mpl-core utils/mod.rs`). FIX: **fetch the asset first, then `transfer()`** (`nft.ts`), which resolves owner+plugins so the delegate path works (verified: on-chain owner → buyer). Both fixes `tsc`-clean. Empirically proved each step now works: createV1 ✓, items insert w/ condition ✓, delegate transfer ✓. **Judah's stuck order `a2d802ee` (SDKTEST-2921) RECOVERED** — Tally `GoQVaViXUe4caBWXWadN2b3jHvdPhCRPqwTf2hPovsRx` on-chain owned by his wallet `HTLt…`, order now `minted`. (Left harmless devnet orphan assets from repro: `BrCj…` merchant-owned, `2JN5…` buyer-owned — not in DB.)
  - 🔴 **SDK-MINT-b (lower pri, hardening)** — the `retry-mints` cron is **daily (07:00 UTC)** — too slow for a money path even for genuine transients. Add inline retry (2–3×) at settle + a minutes-frequency recovery sweep + alert on any paid-no-mint. (Now that the two deterministic bugs are fixed, transients are the only remaining failure mode — but still worth hardening.)
- 🔴 **SDK-PAY (Judah's card concern, confirmed)** — the checkout charged a **raw Stripe card** (`pay_method='card'`), NOT his primary method. Cause: the one-tap `DefaultPayPanel` only supports {Visby SOL balance, saved Stripe card}; his account had neither funded → fell back to manual Stripe `CardElement`. It also uses **Stripe, not Moov** (ties to B1), and never reads the account's real primary payment method (bank/FC/crypto order). FIX: route SDK checkout to the buyer's actual prime method + Moov for cards; never expose a raw Stripe card by default.
- 🔴 **SDK-UX (new — Judah's 2-click vision, extends B4 + SES1)** — SDK checkout should: **recognize the device** (reuse the buyer's persisted Privy session on visby.me → returning buyer skips login; re-auth only after long inactivity via Face ID/passkey per SES1) and show a **2-click Apple-Pay-style confirm sheet** with **3 currencies**: (1) buyer's preferred display currency, (2) what's actually charged from the prime method (e.g. "0.089 SOL" / "$25 from Visa ••42"), (3) what the seller receives ("24.12 USDC"). Tap Pay → biometric → done. Depends on SDK-PAY (prime-method routing) landing first.

## Part-2 duplicates → existing IDs (retest notes confirm Part 1)
A1–A7 (auth) · A8 (Civic gate) · W1 (wallet linking) · W2/W2b/W3 · S1/S2/S3 · L1/L2/L3 · B1 (Moov) · B2→B2a/B2b · B3 (escrow) · B4 (Apple-Pay checkout) · SH1–SH5 · P1/P2/P3 · POL1–POL7. "Admin flag queue" = S2. "Business settings separate" = P1. "Shared URL forces sign-in + 1h session" = S3+SES1.

---

## Fix log (append as items are fixed — most recent first)
<!-- Format: [ID] status — what changed (files, commit) · risk to PASS areas -->

### ROUND 3 EXECUTION (2026-07-13) — PARTIAL, cut off by a usage limit (resets 3:40pm America/Denver)
> Ran an ultracode workflow (recon → implement → verify). Recon/audit finished; **4 of 6 implement agents were killed by the session usage limit mid-run.** Branch `fix/12b-round3` is **tsc-clean and coherent** (I stabilized it) but **NOT deployed** — the auth change is unverified + onboarding isn't built, so it stays on the branch until the resume.

**LANDED + compiling (on branch, awaiting verify — do NOT assume Judah-tested):**
- **Auth (A1/A2/A3/SU1 partial):** `PasswordGate` GUTTED to a passthrough (custom password layer removed); `providers.tsx` → `loginMethods:['email','sms','google','apple','wallet']` + `walletList:['detected_solana_wallets','phantom','coinbase_wallet','okx_wallet','wallet_connect']` (note: Privy 1.99.1 has no 30 named Solana wallet ids — `detected_solana_wallets` auto-surfaces every installed Solana extension, which IS the right mechanism); `login/page.tsx` chooser tweaks; `wallet.ts` touched. **`OnboardingGate` = a PASS-THROUGH STUB I wrote** (the agent that was to build the real name/username/pfp/terms/address/verify-ID/Face-ID steps died first) → SU1 onboarding steps still TODO.
- **S1 search re-rank** (search-engine/synonyms/listings) — landed.
- **Settings (ADM1 + P3d)** (settings/page.tsx) — landed.
- **Item page (S2/L4/QR1/POL1/P3a)** — 111 lines changed, tsc-clean, but the agent hit the limit → **needs a coherence review** before trusting.

**NOT done / reverted:**
- **Wallet page (W1/MOB1)** — agent left a half-finished prop rename that broke tsc → I **reverted** `payment-methods-manager.tsx` to HEAD. Redo on resume.
- **Page-by-page sweep (A8/W2b/S4 currency+borders)** — barely ran (2–3 lines). See audit finding below.

**🔎 AUDIT FINDING (the "new strategy, page by page" — valuable):**
- **Borders are CLEAN.** The audit found **NO hardcoded black/non-token borders** anywhere — every card/chip/header border already uses `var(--glass-border)`/`var(--divider)`/brand accents. So Judah's "black lines" (A8/S4) are **NOT stray black borders** → likely the **selected-state chips not being dark/bold enough** (S4 = contrast, not a black border) and/or scrim overlays. Re-scope S4 to "make selected state clearly darker/bolder," not "remove black borders."
- **Currency is the REAL widespread bug.** Many pages render raw `$`/`USDC` instead of the user's preferred currency via `useCurrency().format`: `dashboard/page.tsx` (never imports useCurrency — ~11 spots), `order/[itemId]` (no import), `p/[wallet]` (imports but never destructures `format`), plus surrounding rows on `item/[id]`, `mint`, `dashboard/seller`, `sdk/checkout`, `business-item`. Full list in the workflow output: `tasks/wu15emc05.output`. **This is the A8/W2 currency sweep's real target list.**

**RESUME PLAN (after 3:40pm Denver):** re-run the workflow's failed legs — finish auth (real OnboardingGate SU1 steps), redo wallet (W1/MOB1), coherence-review the item-page edit, run the currency sweep over the audit's target list. Then tsc + browser-verify login/home/item + deploy → Judah retests.

### ROUND 2 RESULTS + PASSWORD-REMOVAL DIRECTIVE (2026-07-13, Judah retest)
> **2 pass (SES1, S3), rest recycle. HUGE directive: KILL PASSWORDS.** Judah: *"get rid of passwords altogether. just do email/phone codes and face id as an idle passkey. passwords just add friction… but still add all the onboarding steps and social sign ins and wallet sign ins."* This overrides the old locked decision (email+password+2FA) — updated in blueprint + memory.

**✅ PASSED (→ Done):** SES1 (session model) · S3 (shared listing opens logged-out).

**🔑 AUTH OVERHAUL (supersedes A1/SU1/A5/A6/POL4/POL5 — one coherent rewrite):**
- **REMOVE the custom `PasswordGate`** entirely (`src/components/password-gate.tsx` + its 5 `/api/account/password/*` routes + `account-password.ts` become dead). No passwords anywhere. This is what made A5/A6 janky ("trying to do the whole password thing yourself") and never sent reset emails.
- **Auth = Privy-native:** email code **OR** phone code **OR** Google **OR** Apple **OR** external wallet. A6 answer: Privy's OTP IS the recovery — no password reset to build. SU1 answer: Privy verifies email/phone via code up front, THEN onboarding steps run (no password step in between).
- **A2** — social login: wire `loginMethods:['email','sms','google','apple','wallet']` in `providers.tsx` (Judah already enabled Google+Apple in the Privy dashboard; the buttons still need the client-side config).
- **A3** — expand `walletList` from just `['detected_wallets','phantom']` to the full Solana set (~30: phantom, solflare, backpack, glow, coinbase_wallet, etc.) so it isn't "just phantom."
- **A1** — Step-1 chooser (**Sign in** vs **Create account**) as a custom pre-Privy screen; Create-account button needs a **box-shadow**. Sign-in → Privy code → done. Create → Privy code → onboarding.
- **SU1 onboarding (create-account only, after Privy verify):** name → username → description → profile pic → agree to Terms → address → payment info → verify identity (**skippable**, required later to sell) → enable Face ID. Face ID = **app-lock on recognized devices only** (existing `AppLock`), NOT a sign-in replacement. **No "passkey instead of password" option** (POL5 reversal — moot once PasswordGate is gone).
- **POL4** — the always-on 2FA toggle in `security-settings.tsx` stays removed.

**🆕 NEW ITEMS (from Judah's notes):**
- **A9** — **External wallet linking is BROKEN**: linking a wallet still forces an email sign-in AND doesn't actually link. Wallet should be sufficient to sign in on its own (Privy supports wallet-only login). Fix linking + allow wallet-as-sole-login.
- **ADM1** — **Admin entry is too exposed.** Remove the prominent admin block; put a tiny "admin" text link at the **bottom of the Settings page** next to the privacy-policy/legal links — clickable, low-key, only there.

**🔴 RECYCLE (failed retest, with notes):**
- **A7 / P1** — still no business-account toggle in Settings. Build it behind a full verification flow **+ an "are you sure?" confirm screen** (make users understand it's a big deal).
- **A8** — remaining **black lines** + **non-preferred-currency** displays persist. Judah: *"use a new strategy or tool… look page by page."* → a systematic **page-by-page audit** (this round's workflow does exactly that) feeding fixes.
- **W1** — external-wallet-as-payment still says *"…signing is coming soon."* Remove it; make it a real payment method (pairs with A9).
- **W2** — omni-currency still doesn't work (big lift).
- **W2b** — gradient clips persist (folds into the A8 page-by-page sweep).
- **W3** — Judah: *"whats a signiture?"* → it's the wallet's approval popup for an on-chain action; W3 tests that cancelling it doesn't strand the app. **Claude's check, not Judah's** — I verify it, he doesn't need to.
- **S1** — search now returns irrelevant results — re-rank tighter.
- **S2** — **spec locked:** a **⋯ menu next to the heart on the item page** (NOT homepage) → dropdown **Report** → pick a reason → submit (a little 3-step flow). Moderation-queue link stays admin-only ✓.
- **S4** — better, keep going: **selected = darker / higher-contrast / bolder** than unselected; kill the black borders.
- **P3a** — homepage/listing seller names still don't link to the profile.

<!-- prior rounds below -->

### ROUND 3 (2026-07-13) — build unblocked + round-3 fixes verified rendering
> The round-3 work had left the tree **not compiling** (nothing was testable). Fixed the two blockers, then browser-verified.
- **BUILD FIX:** created the missing `src/components/onboarding-gate.tsx` (providers.tsx imported it but it never existed) + the `payment-methods-manager.tsx` drag props reconverged on the desktop `onDragOver` signature (the MOB1 touch-drag work was backed out). **`tsc --noEmit` = 0 errors.** Preview: home renders (grid + nav + onboarding), `/login` renders the two-button split (A1). No hydration crash from the passwordless/provider-tree change. WalletConnect `Failed to fetch` console noise = third-party registry preload in the sandbox, non-fatal.
- **Passwordless overhaul (Judah 2026-07-13 directive "get rid of passwords"):** `password-gate.tsx` neutralized to a pass-through; login is now Privy email/SMS OTP + social + wallet; Face ID stays app-lock-only (Settings). POL5 nudge removed. **SU1 caveat:** `OnboardingGate` is a NON-BLOCKING placeholder — the full profile-setup wizard (name/username/pfp/terms → address/payment → verify-ID → Face ID) is the remaining big lift; kept non-blocking so it can never trap a user mid-auth.
- **Round-3 delivered (🟢 awaiting retest):** A1 (two buttons + Create-account shadow), A2 (social `loginMethods` google/apple wired), A3 (`detected_solana_wallets` + named Solana wallets, no raw pairing URI), **QR1** (Tally QR removed from item page), **S2** (user "•••" menu beside the heart → Report → reason picker → submit; admin moderation-queue link stays admin-only), **ADM1** (admin moved to a small link in the Settings legal footer), **P3d** (Settings wallets section renamed "Visby native wallets"), **S1** (search re-rank via search-engine.ts + synonyms.ts + listings router), **SDKT1** (merchant install tutorial).
- **Reverted / back to TODO:** MOB1 (mobile touch-drag) — backed out to keep the tree consistent; re-do later.
- **Still OPEN (big lifts, not in round-3):** SU1 wizard, A7/P1 business accounts, A9 (wallet-as-sole-login + real linking), W1/W2 currencies, B1 Moov card code, B2a/B2b crypto, B3 escrow + pay-request page, B4 checkout, SH1/N1 label+notifications, SH3–SH5, A8, D1 ratings, N2 badge, PERF1, S4 borders, RATE1, DES1 sweep, CART1.

### ROUND 4b — AUTHENTICATED SWEEP ON PROD (2026-07-13, Judah's session in the shared pane)
- **N1 VERIFIED LIVE on visby.me with real data:** one-time popup "2 orders waiting on a shipping label" (Test, gasp) + pinned cards "You sold Test — create a shipping label / $9.10 on the way — payout releases on delivery" + gradient create-label buttons. Notification deep-link to /request/[id] present.
- **SH1 modal VERIFIED LIVE:** 3-step FROM → PARCEL → RATE & BUY with item thumbnail, address form, weight read-back + edit-listing link, manual-tracking fallback.
- **🎯 REAL BUG FOUND + FIXED + DEPLOYED:** rates returned none (502). Traced through prod DB + a direct AtoShip call: the buyer's ship_address was stored lowercase ("co") and USPS's validator behind AtoShip HARD-REJECTS lowercase state codes (regex) → AddressValidation error instead of rates → "No carrier rates returned." FIX: normalize (trim + uppercase state/country) in the AtoShip adapter's toAddress — the choke point for rates AND labels. Verified directly: same order now returns 18 real rates (USPS Ground Advantage $5.48, UPS, FedEx…). Pushed to main.
- **Design note (minor, for Judah):** pinned notification cards use a visible blue outline border — consider softening to shadow-per-doctrine if it reads as a hard border on retest.

### ROUND 4 — CLAUDE BROWSER SWEEP (2026-07-13, pre-push): verified in the running app, one systemic root cause found+fixed
> Judah: "double check every one of the bugs yourself using the browser… assuming they all pass then push it."
- **🎯 ROOT CAUSE FOUND+FIXED live (the recurring "black borders on selected buttons"):** React drops border-color when a button re-renders across `btn()` variants (secondary→primary): the diff clears the stale borderColor longhand and never re-applies the unchanged `border` shorthand → falls back to `currentColor` (near-black). Runtime-only — invisible to every static sweep, which is why S4/W2b kept recycling. FIX: every `btn()` variant now sets the borderColor longhand explicitly (`src/lib/ui.ts`). Verified live: active Filters chip borderColor = fully transparent, gradient + glow only.
- **Browser-verified PASS:** A1 (two buttons+shadow) · A2 (Google+Apple in the Privy modal) · S1 both directions ("succulent"→exactly Ikea plant; "watch"→clean "No matches found"+Clear filters) · S2 (••• beside heart → "Sign in to report" signed-out state) · S3 (every page served logged-out) · S4 (filter applies, chip gradient, no border) · L4a (SN in Tally panel) · L4b (desc immediate) · L4c (no "≈ USDC") · QR1 (gone) · P3a (seller + provenance rows link to /p/) · **POL1 ("5 owners" rendering on the mint→J→T→J→T chain — the exact 1→2→1 rule)** · D1 (shield tier, no stars, "No ratings yet") · "(USD)" filter label · CART1+`/request` signed-out states · **SDKDEMO end-to-end (real merchant+session via the live SDK API, visby-button rendered, serial logged → proves SDK1–SDK3)** · PERF1 (:active rules live in served CSS) · W2b/DES1 light+dark · onboarding v3 slides.
- **⚠️ A3 = PARTIAL, capped by SDK version:** reproduced "just Phantom" — Privy 1.99.1 has NO Solflare/Backpack WalletListEntry ids and its WalletConnect is EVM-only; `detected_solana_wallets` lists whatever is INSTALLED. A ~30-wallet discovery list requires the **Privy 2.x upgrade** (major version, touches signing hooks/money path) → its own tested round. DECISION FOR JUDAH.
- **Also fixed during sweep:** corrupted `.next` cache (500s — cleared+rebuilt) · `DEMO_CATALOG` illegally exported from the demo route (Next route-type check).
- Auth-gated flows (SU1 steps, settings cluster, wallet linking, checkout live-fire, label flow) can't be browser-tested by me — entering login codes isn't something I can do even with permission; code-verified → Judah's retest.
- **Final gate: tsc 0 · vitest 231/231 · PUSHED per Judah's conditional go.**

### ROUND 4 — WAVE 2 COMPLETE (2026-07-13): sweeps + money rails, all 5 workflow agents merged
- 🟢 **ERR1** — new `src/lib/friendly-error.ts` (never returns undefined/[object Object]/stack/JSON; maps network/auth/429/wallet-reject/insufficient-funds/bad-OTP) swept through ~60 files: mint/sell, profile/settings/wallet, buy-crypto/checkout rails, item, disputes/reviews, request, social/cart routers, admin pages, reports. Covers A5's clean-error half.
- 🟢 **DES1/W2b** — systematic sweep: 7 gradient-clip fixes (border-box clip/origin on seller toggles, business toggle, buy-crypto pills/rows, admin chips), black-line kills (profile transfer row + more → shadow-not-border), centering fixes; **CurrencyPicker wired into Settings + wallet currency rows** (no more 103-button wall). Pages verified rendering.
- 🟢 **PERF1** — global instant press feedback (:active scale + 80ms transition, reduced-motion fallback), touch-action manipulation (kills mobile tap delay), tap-highlight cleanup, btn()/cta() userSelect, nav prefetch. tsc + prod build clean.
- 🟢 **B1 (code done; flag still OFF)** — Moov = sole card rail when flag on (killed the silent Stripe PaymentIntent that fired even on the Moov tab); **saved-card one-tap**: moov_cards table (APPLIED live), card-token reuses the buyer's Moov account (was: new anonymous account every call), /api/moov/cards GET, charge accepts card_id + saves card-on-file only after a successful charge, "Pay with {brand} ····{last4}" + "Use a different card". Fail-soft pre-migration; flag-off byte-identical. MAIN-THREAD REVIEWED: fulfill-only-on-completed + shared fulfillPurchase + rate limits intact.
- 🟢 **B2b (flag-dark)** — Coinbase Commerce rail: lib (create charge + HMAC timing-safe fail-closed webhook verify), create-charge (authed/ban-gated/rate-limited/server-priced via resolveCheckoutPrice), webhook fulfills on charge:confirmed via shared fulfillPurchase echoing metadata price (never re-resolves), "More crypto" tab gated on NEXT_PUBLIC_COINBASE_ENABLED. NEEDS (Judah): Coinbase Commerce account + COINBASE_COMMERCE_API_KEY + COINBASE_COMMERCE_WEBHOOK_SECRET, then the flag. MAIN-THREAD REVIEWED: fail-closed verify + metadata echo + idempotent fulfill confirmed in code.
- **Gate run:** tsc 0 · vitest 231/231 · all key pages 200 · home screenshot clean (onboarding v3 w/ omni-currency slide, gradients covering shapes).

### ROUND 4 — WAVE 1 COMPLETE (2026-07-13): 16 agents merged, tsc clean, money-path reviewed
> All 12 wave-1 agents + 4 restore agents landed. **Incident:** a subagent ran `git stash/reset` in the shared tree mid-wave, temporarily wiping uncommitted work — everything recovered (stash + context + re-dispatch); wave-2 prompts now ban git state ops. Migrations applied LIVE to prod via Composio: `cart_items`, `business_verifications` (+profiles.self_ship), `sdk_demo_config` — verified RLS-on.
- 🟢 **SU1** — 6-step wizard (terms→profile/username/pfp→address→payment→verify-ID skippable→Face ID) behind OnboardingGate; fires for fresh signups/username-less accounts; never traps. 🟢 **A1** (shadow) · **A2** (google/apple in loginMethods) · **A3** (solana-only walletList) restored.
- 🟢 **A7/P1/A8** — BusinessSettings in Settings w/ EIN verification + are-you-sure confirm + self-ship toggle; sell page fully KYC-gated w/ reason; merchant create auto-flags business + stub verification. KYC account-type loophole closed by the parallel worktree session (admin approval route is sole writer).
- 🟢 **A9/W1** — real Privy `useLinkAccount` wallet linking; watch-only demoted to ETH/BTC; zero "coming soon". **MOB1** — pointer-events drag (mouse+touch). *(Follow-up: checkout pay-FROM-external-wallet unbuilt.)*
- 🟢 **L4a/b/c** — SN → Tally panel; 4-line desc clamp; no "≈ USDC" (viewer pref currency + seller currency only when set+differs). **QR1** removed. **S2** — "•••" next to heart → 3-step report flow. **P3a** — item-page seller links; message-LIST names unlinX. **POL1** — display uses history.length; **DB verified healthy via Composio** (trigger live, counts in sync incl. a 5-transfer item) → POL1 retests true once B3 transfers work.
- 🟢 **D1** — sales-scaled stars killed on /p/[wallet]; "No ratings yet" until real reviews.
- 🟢 **SH1/SH2/N1** — ship-label-flow 4-step modal (real rates, honest test-mode); pinned "You sold X — create label" + payout cards + one-time popup; Sales rows get Create-label. **P3b** — code path verified complete (origin HAD it; if it fails live retest I test with real data). **B3-preview** — inline pay-request cards in threads; notif links → /request/[id].
- 🟢 **B3** — dedicated /request/[id] page (accept/decline/receipt, retry-safe signing); **devnet transfer FIXED**: blockhash-expiry resend loop (idempotent bytes) + bounded server confirm retry — the "devnet wasn't transferring" root causes.
- 🟢 **B2a** — crypto-buy root causes: transient wallet-mismatch signature + deprecated confirm throwing AFTER money moved; both fixed via shared sendSol; /api/sol-pay verified sound, untouched. **B4** — Apple-Pay summary (big pref-currency price, "Using: X SOL", "Seller receives: Y", primary-method auto-select + "Change"), sellerCurrency threaded from item page.
- 🟢 **CART1** — cart_items live; cart router (authed, own-item guard, auto-prune); /cart page w/ sequential Buy-all via existing CheckoutModal; AddToCartButton built (wiring into item page = wave 2 check). **SDKDEMO** — /sdk/demo burner shop (3 fake products, real merchant-API path, serial log). **L3** — scripts/test-bulk-serials.mjs.
- 🟢 **RATE1** (limits added where missing) · **POL6** (root cause: UA-drift fingerprints → stable platform+browser-family, upsert-in-place, self-cleaning) · **POL3** (old address preserved into book).
- 🟢 **W2** — 103 currencies (90 fiat via open.er-api + 13 crypto via oracle), backward-compatible; CurrencyPicker built; onboarding slide added. *(Settings/wallet picker wiring = wave 2 DES1.)*
- 🟢 **SH3/SH4/SH5** — confirm-delivery + celebration strip; in-app ratings (CONFLICT-guarded, (order_id,reviewer) unique); AddressForm country select + per-country postal rules, foreign accepted.
- ✅ **SEC1** — clean (pages/bundle/APIs/dotfiles). **SDKT1** superseded by the worktree session's committed tutorial (0b9e2b3, on origin).
- **WAVE 2 (running):** ERR1 sweep · DES1+W2b sweep + currency-picker wiring · PERF1 · B1 Moov default rail + saved-card · then B2b Coinbase (flag-dark). **Still pending after: W3 (needs signing session), final tsc/browser pass, deploy for retest.**

### ROUND 4 SWEEP (2026-07-13, in progress) — Judah: "fix every item on the list"
> **Wave 1 = 12 parallel Sonnet subagents launched** with exclusive file ownership: SU1 wizard · A7/P1/A8 business · A9/W1/MOB1 wallets · L4/POL1/P3a item+cards · SH1/SH2/N1/P3b/B3-preview notifications · B4/B2a checkout · B3 pay-request page+devnet transfer · CART1 · SDKDEMO+L3 · RATE1/POL6/POL3 · W2 omni-currency · SH3/SH4/SH5. Money-path diffs (checkout, transfers) get main-thread review before ship. **Wave 2 after merge:** ERR1 sweep, DES1+W2b sweep, PERF1, B1 Moov code, B2b Coinbase (flag-dark).
- **SEC1** 🟢 — Claude ran it (dev server): SSR pages (/,/login,/settings,/merchant,/sdk,/help), JS-visible env, public API responses (rates/shipping-config/getListings), dotfiles (.env/.git → 404) — ALL CLEAN; only match = the `sk_visby_YOUR_SECRET` placeholder in /sdk docs copy. Re-verify once on prod after deploy.

### ROUND 3 fixes (2026-07-13) — 🟢 awaiting Judah retest (NOT yet deployed)
> Built this session. Tree is GREEN (`tsc` 0 errors) + home/login browser-verified. **These need a deploy before they're retestable on visby.me.**
- **BUILD FIX** — round-3 work had left the tree not compiling: created missing `src/components/onboarding-gate.tsx` (providers.tsx imported it) as a safe non-blocking pass-through; payment-methods drag props reconverged after the MOB1 touch-drag was reverted. `tsc` clean again.
- **N2** 🟢 — inbox unread badge now truly centered (was `lineHeight:'12px'` + `textAlign` fighting flex centering; now `lineHeight:1` + `box-sizing:border-box`, holds for 1/2-digit/99+). File: `src/components/layout/bottom-nav.tsx`. *(Sonnet subagent.)*
- **S4** 🟢 — removed the harsh dark border on the active "Filters" toggle (`src/app/page.tsx` line ~266); it now reuses the filled `btn('primary')` gradient like every other selected chip. All other chips already used the clean `btn()` helper. NOTE: `/marketplace` is just a redirect stub — search lives on the home page. *(Sonnet subagent.)*
- **D1** 🟢 — seller "rating" was 1–4 stars scaled by SALES count (the `trustTier` block on `src/app/p/[wallet]/page.tsx`). Replaced the sales-scaled stars with a neutral shield/tier icon (no star, no implied number), and added an explicit "No ratings yet" state; the real numeric average (from the `reviews` table via `reviews.getReputation`) only renders once `rep.count>0`. *(Sonnet subagent.)*
- **SDKT1** 🟢 — "How to install" opt-in tutorial on the merchant page (built earlier this session; see SDKT1 entry).
- **Earlier round-3 (on disk, pre-session):** A1 (two-button login + Create-account shadow), A2 (Google/Apple in Privy `loginMethods`), A3 (Solana-only wallet declutter), passwordless overhaul (PasswordGate neutralized → Privy OTP; POL5 nudge removed; Face ID = app-lock only), QR1, S2, ADM1, P3d, S1 re-tune. *(These were on disk at session start — verify against Judah's retest.)*

### ROUND 1 RESULTS + new requirements (2026-07-09, Judah retest)
> Verdict on the 19 shipped items + a batch of new/clarified requirements. **6 pass, 13 recycle.** The checklist was also wrong — it only showed the 19 shipped items; the real roster is ~50 (all now in the interactive checklist).

**✅ PASSED (→ Done):** L1 · L2 · P2 · POL2 · POL4 · P3c *(P3c toggles "look amazing" BUT gradient clips persist site-wide, esp. wallet + settings buttons → that's **W2b**, still open).*

**🔴 RECYCLE (failed retest, with Judah's notes):**
- **S1** — over-corrected: results are now "kinda just irrelevant stuff." Tighten relevance (keep the synonym expansion but re-rank; don't return loosely-related junk).
- **S2** — **no visible user flag button** (it exists but is `compact`/subtle + hidden when logged out). Make a clear user-facing "Report/Flag" action. CONFIRMED: the "Review in moderation queue" link IS admin-only (`isAdmin`-gated) — that part is correct, leave it.
- **P3d** — REFRAME: the whole **Payment Methods** section should NOT be in Settings (it's already on the wallet page) → **remove it from Settings**. Rename Settings' "wallets" label → **"Visby native wallets."** And support **many wallet currencies, not just SOL/USDC** (ties to W1 + W2).
- **A1 / SU1** — the two-button split still isn't on the **Privy modal** ("Log in or sign up… your@email.com… Continue with a wallet") — it drops you straight into email; Create-account button needs a shadow; signup skipped all the steps. **FULL FLOW SPEC (Judah, authoritative):**
  - **Step 1:** choose **Sign in** OR **Create account**.
  - **Sign in →** Step 2a: email + password (traditional). Step 3a: Privy 2FA. Done. *(No Face-ID step — they just signed in on a new device.)*
  - **Create account →** Step 2b: email + create password + re-enter password. Step 3b: Privy 2FA. Step 4b: name, username, description, profile pic, agree to all terms. Step 5b: address + payment info. Step 6b: verify identity (**skippable now**, required later to sell). Step 7b: turn on Face ID.
  - **Face ID = app-lock on recognized devices only** (stops re-login like a bank app); it is **NOT** a sign-in replacement. **There is NO "sign up with a passkey instead" option** → this **REVERSES POL5**.
- **A3** — still too few wallet connections; "I can't sign up on my own site." Add many external-wallet options; make it easy/compatible for an average person.
- **S3** — shared listing URL **still won't open logged-out.** Not fixed. (The 1h-TTL half is superseded by SES1 anyway.)
- **POL5** — **REVERSED**: remove the passkey nudge entirely (see A1/SU1 — Face ID is app-lock only).
- **POL3** — (a) collect **address during sign-up** (Step 5b); (b) typing a new address **didn't save the old one** to the book; (c) the **two address managers in Settings still aren't merged** (the settings address DID update — but merge them). 
- **POL6** — active-sessions dedupe still failing (more devices than owned).
- **SH2** — nothing changed: still **no seller tracking popup, no notification link, no way to track** → folds into **SH1 + N1** (build the seller shipping/label + tracking UI, not just surface a field).
- **P3a** — names **still don't link** to profiles. The one that works (the sender name in a **message room header**) is correct; but the plain name inside a **message list should NOT link to a profile** (go to the thread, avoid confusion). **Primary concern: names on the homepage / listings must link to the seller's page.**
- **P3b** — Accept/Decline works ✅, but the offer notification **still doesn't say which product** it's for. Fix the "for {item}" label.
- **POL1** — owner count (transfers) **still not working** on item page + thumbnails.

**🆕 NEW / CLARIFIED requirements (added to roster):**
- **L4 (new)** — Listing-page display cleanup: (a) the **"SN: •••••••• · visible to owner"** belongs on the **Tally**, not in the listing area; (b) show the **first 4 lines of the description by default** (then expand); (c) the price line **"A$153.00 ≈ $100.00 USDC"** must show the **seller's preferred currency**, never USDC.
- **B4 (expanded)** — Apple-Pay-style checkout preview: **big bold gradient price = BUYER's preferred currency.** If the spend source differs, show small light-grey **"Using: 0.89 SOL"** (actual wallet/bank amount) and small light-grey **"Transferring to: 72 USD"** (seller's currency). Two (or three) currencies only — **never inject USDC** for a non-USD user. Don't clutter.
- **B3 (expanded)** — Pay requests: build a **dedicated pay-request page** AND an **in-message preview** (accept/decline inside the message app). **No small floating window on the pay page.**

**Standing correction:** the interactive checklist must show **ALL** items (pass/done, recycling, not-started, big-lifts, blocked), not just the freshly-shipped subset. Fixed this round.

<!-- prior fixes below -->

### Wave 2 (2026-07-08) — all 🟢 awaiting Judah retest
- **A1** 🟢 — login now shows **two buttons** (Sign in `disableSignup:true` / Create account). Root-caused the "one method skips password" bug: `useVisbWallet` only read the live connector, which is transiently empty for external wallets → PasswordGate skipped; now falls back to Privy's persisted `getSolanaAddress(user)` so **every** method routes through create-password. Files: `login/page.tsx`, `lib/wallet.ts`, `password-gate.tsx`.
- **A3** 🟢 — external-wallet signup trimmed to Solana-only direct-connect (`walletList:['detected_wallets','phantom']`, `walletChainType:'solana-only'`) → no raw WalletConnect pairing code, fewer clicks. File: `providers.tsx`. *(Verify with Phantom/Solflare installed.)*
- **S3** 🟢 — confirmed item pages are already public to logged-out visitors (no forced sign-in found; buying still gated). Added a **~1h session TTL** to the password gate (localStorage timestamp + 60s/visibility re-check → re-prompt after an hour). File: `password-gate.tsx`.
- **POL5** 🟢 — sign-up now nudges **"Use a passkey instead"** (Privy `linkPasskey`) before the password form, with a "use a password instead" opt-out; gated on WebAuthn support. File: `password-gate.tsx`. ⚠️ needs Privy dashboard passkeys enabled to actually complete.
- **POL3** 🟢 — checkout/Settings shipping address now **saves into the Address Book** (added `upsertDefaultShippingAddress` to the ship-to route → `shipping_addresses` is the single source, `profiles.ship_to` stays the fast-read snapshot). File: `api/buyer/ship-to/route.ts`.
- **POL6** 🟢 — **Active sessions deduped** by device fingerprint (Privy issues a new session_id per re-login); revoke now clears all sibling sessions for a device so it doesn't reappear. File: `lib/device-sessions.ts`.
- **SH2** 🟢 — seller's Sales view now shows a labeled **Carrier + Tracking** block (data was already returned; UI gap). Files: `dashboard/page.tsx`, `order/[itemId]/page.tsx`.
- **P3(a)** 🟢 — every user name in notifications/messages/sales now **links** to `/p/[wallet]` (or `/profile` if you). File: `dashboard/page.tsx`, `order/[itemId]/page.tsx`.
- **P3(b)** 🟢 — Notifications tab has an **Offers** section: "Offer on {item}", amount vs list, inline **Accept/Decline** → `/api/offers/respond`. File: `dashboard/page.tsx`. *(Follow-up: offers don't yet write to the notifications table — sourced live from `/api/offers/list`.)*
- **POL1** 🟢 — item page counts **transfer events** (`history.length`) not distinct wallets; added a DB **trigger** that keeps `items.transfer_count` = ownership-event count (+ backfill, **applied to prod**) so the thumbnail "N owners" is correct for buy→sell→buyback. Files: `item/[id]/page.tsx`, `listing-card.tsx`, `migration_transfer_count_trigger.sql`.

### Wave 1 (2026-07-08) — all 🟢 awaiting Judah retest
- **S1** 🟢 — empty/blank search now returns a graceful empty result instead of the whole catalog; broadened relevance (Datamuse `rel_syn` + `rel_trg` "succulent→cactus" associations feed the primary Orama path, not just the ilike fallback). Files: `src/server/routers/listings.ts`, `src/server/lib/search-engine.ts`, `src/server/lib/synonyms.ts`. Filters/sort untouched.
- **L1** 🟢 — can no longer publish with a missing field; **at least one photo now required** (was unchecked), plus title/serial/category/(condition)/price-when-listing. Submit disabled + inline "Add …" hint + handler guard. Files: `src/app/dashboard/seller/page.tsx`, `src/app/mint/page.tsx`.
- **S2** 🟢 — removed the admin "authenticate/flag" action button from the item page (provenance is passive-display only); the **user** ReportButton→`/api/reports`→admin queue path was already correct, and I **added a proactive admin notification** (`report_filed` → all admins → `/admin/reports`) on every new report. Files: `src/app/item/[id]/page.tsx`, `src/app/api/reports/route.ts`.
- **L2** 🟢 — owner can now **add photos + edit description** on a live listing; **title stays locked** (read-only w/ lock caption). New append-only `/api/items/update` (owner-only, never removes originals) + `items.extra_image_urls` column (**migration applied to prod**). Thumbnail strip shows extra photos. File: `src/app/item/[id]/page.tsx` + new route + migration.
- **P2** 🟢 — added the **@username** line (Instagram spot, under display name) on `/profile` and `/p/[wallet]`; removed the **redundant name+bio box** in the profile Public-View tab. Files: `src/app/profile/page.tsx`, `src/app/p/[wallet]/page.tsx`.
- **POL2** 🟢 — Delete-account confirm button was black-on-black in light mode → forced white text on the danger background. File: `src/components/security-settings.tsx`.
- **POL4** 🟢 — removed the misleading always-on **2FA toggle** (real 2FA behavior untouched). File: `src/components/security-settings.tsx`.
- **P3(c)** 🟢 — toggle switches: gradient now covers the whole track (border-box clip/origin/size) + inset "carved" shadow. Files: `src/app/settings/page.tsx`, `src/components/security-settings.tsx`.
- **P3(d)** 🟢 — Payment Methods "Crypto (SOL, USDC) — SOON" → **"Active"** (SOL/USDC checkout works). File: `src/app/settings/page.tsx`.

## Retest queue (🟢 awaiting Judah)
_(round 1 verdicts are in; next 🟢 batch will be the round-2 fixes.)_

## Recycling after round 1 (🔴 back in the fix rotation, with Judah's notes above)
S1 (over-broad) · S2 (user flag button) · P3d (remove from settings + rename + multi-currency) · A1/SU1 (full flow) · A3 (more wallets) · S3 (still gated) · POL5 (REVERSE — remove nudge) · POL3 (signup address + save-old + merge) · POL6 (dedupe) · SH2→SH1/N1 (build the UI) · P3a (homepage/listing names) · P3b (which-product label) · POL1 (owner count)

## Done (✅ verified by Judah)
- **Round 1:** L1 · L2 · P2 · POL2 · POL4 · P3c *(toggles pass; gradient-clip → W2b)*
- **Round 2:** SES1 (session model) · S3 (shared listing opens logged-out)

## Notes / decisions needed from Judah
- ~~**B2**: Coinbase Commerce vs. SOL-as-crypto-rail — which?~~ **RESOLVED 2026-07-09: BOTH** (B2a fix SOL rail now, B2b Coinbase Commerce later wave).
- **Moov flip**: still HELD — say "flip Moov" once Moov-dashboard capabilities + `visby.me` allowlist are confirmed (B1 code will be built first regardless).
- **SDK separate testing**: instructions + Chrome-Claude prompts delivered as `docs/sdk-test-instructions.pdf` (2026-07-09).

### Composio config audit — 2026-07-08 (I checked prod via Composio; the config is NOT the gap)
- **Resend domain `visby.me` = already VERIFIED.** `RESEND_API_KEY` (working restricted send key) + `EMAIL_FROM` (verified from-address) + Privy creds are ALL set in Vercel. So **A5/A6 is not a config/domain problem** — nothing to change there.
- **A4 is the keystone bug — and it's Privy-dashboard-only (no Composio API exists for Privy config).** Root cause chain: email login isn't auto-creating the **Solana embedded wallet** → downstream, `resolveEmail()` looks the user up by `getUserByWalletAddress(solanaWallet)`, so with no Solana wallet there's **no email to send the reset code to** → **A4 causes A5 + A6.** **FIX = enable "Solana embedded wallets" in the Privy dashboard.** That one toggle should fix A4, A5, A6 together.
- **A2 (Google/Apple) = Privy dashboard too** (enable OAuth providers). No Composio path; there is no Privy toolkit in Composio.
- **B1 (Moov): all Moov creds are in Vercel** — `MOOV_SECRET_KEY`, `MOOV_PUBLIC_KEY`, `MOOV_PLATFORM_ACCOUNT_ID`, and **`MOOV_WEBHOOK_SECRET` (re-set by me 2026-07-08 from Judah's paste)**. The ONLY missing env is **`NEXT_PUBLIC_MOOV_ENABLED=1`** — still **HELD**. Two reasons: (1) flipping it swaps the currently-working Stripe card fallback for Moov, and if Moov's **sandbox capabilities / `visby.me` domain-allowlist** aren't confirmed in the Moov dashboard, card checkout breaks; (2) B1 also needs **CODE not yet built** — make Moov the default card rail + **auto-select the saved default card** (that saved-card auto-use is the actual fix for Judah's "had to re-type the card" complaint). Plan: build the Moov card code first, then flip the flag once Judah confirms Moov capabilities are on. **Say "flip Moov" to flip immediately.**
- **PASS items I must not break** (flag if a fix risks them): log-in/out session restore, onboarding screens, unlink wallet, wallet balance, filters/sort, listing detail render, mint, delist, fee math, double-click-buy dedupe.
