# Visby — QA Fix Cycle 12b (Phase 12 · batch B)

> Judah's manual QA pass (website + mobile, 2026-07-08). This is the working fix list. **Part 1 loaded.**
> **Part 2 will be appended when Judah sends it.**

## How this loop works (flashcards)
1. Every item below has a stable **ID** and a **status**.
2. Claude fixes items, marks them 🟢 **fixed — awaiting retest**, and writes exactly what changed in the **Fix log**.
3. Judah retests. Working → ✅ **done** (checked, drops out of the active rotation, kept as a record). Not working → back to 🔴/🟡 with a note, and it **recycles** until it passes.
4. We keep cycling until a full clean run, then the batch closes.

## Status legend
- 🔴 not started
- 🟡 in progress (being fixed now)
- 🟢 fixed — awaiting Judah retest
- ✅ done — Judah confirmed working
- ⚠️ **blocked on Judah** (dashboard/config/keys — code can't fix alone)
- 🅿️ big lift — staged (architectural; broken into sub-steps below it)

## Triage (how the work is ordered)
**Wave 1 — isolated, buildable now (low risk):** S1, L1, L2, S2, P2, plus polish POL2/POL4/POL_toggle, "crypto SOON" label.
**Wave 2 — money-path & careful:** B1 (Moov card), B3 (escrow + pay-request popup), SH1–SH5 (shipping/escrow/ratings UI), B4 (Apple-Pay-style checkout).
**Wave 3 — big architectural lifts:** A7/P1 (business KYC), W2 (100-currency omni system), S1-semantic, A8 currency-on-listing.
**⚠️ Blocked on Judah config (I'll flag + prep, you flip):** A2 (social login = Privy providers), A4 (embedded wallet = Privy Solana toggle), A5/A6 (reset email = Resend domain verify), B1 (Moov enable).
**Clarify:** B2 "Coinbase Commerce" — never built; the real crypto path is SOL/USDC/Li.Fi. Confirm whether you want Coinbase Commerce at all.

---

# PART 1

## Authentication & Onboarding
- 🔴 **A1** — Sign-in must force a password on **every** method; split the entry into two clear actions: **Sign in** and **Create account** (creating from login still allowed — just make it feel like two options).
- ⚠️ **A2** — Add **social login (Google / Apple)** — neither is an option now. *(Needs Privy dashboard: enable Google/Apple OAuth providers; then wire the buttons.)*
- 🔴 **A3** — External-wallet (Phantom/Solflare) sign-up works but has **too many steps** and exposes a code/command mid-flow. Reduce to the fewest clicks from "Sign in" → done; hide the raw code/command.
- ⚠️ **A4** — Email users' **embedded Solana wallet isn't auto-creating** (new account got no wallet). *(Prime suspect: Privy dashboard "Solana embedded wallets" toggle — the known silent-createWallet hang. Verify code path too.)*
- 🔴/⚠️ **A5** — Wrong password / bad code should show a **clean error, not a raw dump**, AND the **password-reset code isn't sending**. *(Reset email likely blocked on Resend domain verification; also audit the reset-request route.)*
- ⚠️ **A6** — **Password reset / recovery** end-to-end doesn't work. *(Same Resend dependency as A5 + confirm the confirm-route.)*
- 🅿️ **A7** — **Business vs. personal**: move the business switch **out of the sell page into Settings**, behind a **full business-verification flow** (EIN, business info, proof — legality/security), and let business accounts **toggle off Visby shipping and self-ship**.
- 🔴 **A8** *(partial)* — Civic gate blocks listing but **doesn't say why** → block the whole list page for unverified users with a reason. **One account per person** (no re-verification loop). On the listing page, show the price in the user's **preferred currency (translated), not just USDC**, and replace any **black-outlined elements** with the site's white-bubble-shadow style.

## Wallets & Linking
- 🔴/⚠️ **W1** — **Linking a second external wallet is broken** and should be more than a destination address — it should be a **payment method too** (redirect into the wallet-linking flow). Remove the "coming soon."
- 🅿️ **W2** *(partial)* — **Omni-currency**: support ~**100 currencies (crypto + fiat)**, any settable as **preferred**, users **auto-receive in their preferred currency**. Today only ~8 and it's clunky. Add a **sign-up info popup** explaining the omni-currency system. *(Copy-address button: noted not necessary.)*
- 🔴 **W2b** — **Gradient coverage sweep**: some gradients clip / don't cover the full shape. Do a site-wide once-over and fix every clipped gradient.
- 🟡 **W3** *(partial)* — "Reject a signature cancels cleanly" — Judah can't test. Claude to attempt via browser/Composio if possible (creds provided out-of-band), else skip silently.

## Marketplace — Browsing & Search
- 🟢 **S1** — **Empty search returns everything** (should be graceful/empty). Also **tolerance too narrow** (search "succulent" → a cactus won't show). Improve relevance; AI/semantic engines welcome.
- 🟢 **S2** — **Counterfeit flag is inverted + wrong button**: remove the "authenticated" button entirely (we show provenance transparency, not authentication). The **flag** should be a **user** action that **notifies admins to review/take down** — currently admins flag and users see. Flip it.
- 🔴 **S3** — **Shared listing URL forces sign-in** for logged-out users → must open publicly. **Session: stay signed in ~1 hour** before re-auth.

## Listing Creation
- 🟢 **L1** — **Required-field validation is missing** — listed with **no photo**. Enforce all required fields (incl. at least one photo) before publish.
- 🟢 **L2** — **Edit a live listing**: currently price-only. Allow **adding photos** (add only, can't remove originals) and **editing the description**. **Title stays locked** (tied to serial).
- 🅿️ **L3** *(partial)* — **Bulk serial logging** (business) — Judah can't test. Claude to set up a testable path/walkthrough.

## Purchases & Sales
- ⚠️/🅿️ **B1** — **Card must run on Moov, never Stripe.** Card checkout silently falls back to Stripe's CardElement because Moov is gated off → no saved-default-card, re-typing card each time (the whole point of Moov). Stripe should only touch **bank/money-management**, never a card. *(Enable Moov (sandbox config + `NEXT_PUBLIC_MOOV_ENABLED=1` + `MOOV_WEBHOOK_SECRET`) AND make Moov the default card rail; saved default card auto-selected.)*
- ❓ **B2** — "Buy with crypto (**Coinbase Commerce**)" failed. **Coinbase Commerce was never built** (parked). Real crypto path is SOL/USDC/Li.Fi. **Decision needed:** build Coinbase Commerce, or treat SOL as the crypto rail and fix that flow?
- 🅿️ **B3** — **Escrow untestable**: sales didn't go through, **devnet not actually transferring**, and the **pay-request popup in chat is broken** (blank redirect to a dead pay page, disappears on back). Fix the pay-request popup; get a devnet money transfer working end-to-end so escrow can be verified on-chain.
- 🅿️ **B4** *(partial)* — **Unified Apple-Pay-style checkout**: show only **preferred currencies** (secondary/native currency smaller below), **auto-pay with primary method**, with a viewer screen to change. Model the UI on Apple Pay.

## Shipping & Delivery
- 🅿️ **SH1** — **Label generation has no UI.** Build a **seller popup walkthrough**, plus a **notification-center button** (link back to that popup) with **order-progress info** so sellers know where their money stands.
- 🔴 **SH2** — **Tracking number** shows to the **buyer but not the seller** — show it to both.
- 🅿️ **SH3** — **Delivery confirmation → escrow release + ratings prompt**: no UI in-app. Build it.
- 🅿️ **SH4** — **Ratings**: submit / display on profile / can't-rate-twice — **no UI**. Build it.
- 🅿️ **SH5** — **Invalid / foreign address handling** — no UI. Build it.

## Profiles, Settings & Account
- 🅿️ **P1** — **Business account settings must be fully separate** from personal (no bleed-over) and easy to switch to. **SDK-page sign-up → auto-create a business account** and route through the business-verification steps. *(Pairs with A7.)*
- 🔴 **P2** *(partial)* — **Username doesn't display** anywhere; the **redundant name+description box** on the profile page must go (no redundancy). **Add the username where Instagram puts it**; clean up the profile page.
- 🔴 **P3** *(partial)* — **Notifications/settings polish**: (a) tapping a user's name doesn't always link to their page → it should; (b) an **offer** notification should say **which product** it's for and offer an **Accept** action; (c) **toggle switches look bad** — the white needs **inset/indented shadow**, and the gradient **clips** (doesn't cover the whole toggle); (d) **Payment Methods** shows "Crypto (SOL, USDC) — SOON" → should be **now** (make it work, or change the view to reflect reality).

## Polish (passing items, tester notes)
- 🔴 **POL1** — **Provenance owner count**: every transfer counts as an owner (1→2→1 = **3 previous owners**), and show the owner/purchase count on the selling thumbnail.
- 🟢 **POL2** — **Delete button** is black text on a black button → unreadable. Fix contrast (keep the rest of its look).
- 🔴 **POL3** — **Shipping address fill-in doesn't save to the address book** → make address entry + address book **one setting**.
- 🟢 **POL4** — **Remove the 2FA toggle** — 2FA is always-on and can't be disabled, so the setting is misleading. Remove it.
- 🔴 **POL5** — **Passkey**: on sign-up, offer to use a **passkey instead of a password** (default nudge). Keep the existing Settings passkey control.
- 🔴 **POL6** — **Active sessions** lists **more devices than owned / duplicates** → dedupe to real sessions.
- ✅-note **POL7** — Declined-card test worked but ran on Stripe → folds into **B1** (must be Moov).

---

## Fix log (append as items are fixed — most recent first)
<!-- Format: [ID] status — what changed (files, commit) · risk to PASS areas -->
### Wave 1 (2026-07-08) — all 🟢 awaiting Judah retest
- **S1** 🟢 — empty/blank search now returns a graceful empty result instead of the whole catalog; broadened relevance (Datamuse `rel_syn` + `rel_trg` "succulent→cactus" associations feed the primary Orama path, not just the ilike fallback). Files: `src/server/routers/listings.ts`, `src/server/lib/search-engine.ts`, `src/server/lib/synonyms.ts`. Filters/sort untouched.
- **L1** 🟢 — can no longer publish with a missing field; **at least one photo now required** (was unchecked), plus title/serial/category/(condition)/price-when-listing. Submit disabled + inline "Add …" hint + handler guard. Files: `src/app/dashboard/seller/page.tsx`, `src/app/mint/page.tsx`.
- **S2** 🟢 — removed the admin "authenticate/flag" action button from the item page (provenance is passive-display only); the **user** ReportButton→`/api/reports`→admin queue path was already correct, and I **added a proactive admin notification** (`report_filed` → all admins → `/admin/reports`) on every new report. Files: `src/app/item/[id]/page.tsx`, `src/app/api/reports/route.ts`.
- **L2** 🟢 — owner can now **add photos + edit description** on a live listing; **title stays locked** (read-only w/ lock caption). New append-only `/api/items/update` (owner-only, never removes originals) + `items.extra_image_urls` column (**migration applied to prod**). Thumbnail strip shows extra photos. File: `src/app/item/[id]/page.tsx` + new route + migration.
- **P2** 🟢 — added the **@username** line (Instagram spot, under display name) on `/profile` and `/p/[wallet]`; removed the **redundant name+bio box** in the profile Public-View tab. Files: `src/app/profile/page.tsx`, `src/app/p/[wallet]/page.tsx`.
- **POL2** 🟢 — Delete-account confirm button was black-on-black in light mode → forced white text on the danger background. File: `src/components/security-settings.tsx`.
- **POL4** 🟢 — removed the misleading always-on **2FA toggle** (real 2FA behavior untouched). File: `src/components/security-settings.tsx`.
- **P3(c)** 🟢 — toggle switches: gradient now covers the whole track (border-box clip/origin/size) + inset "carved" shadow. Files: `src/app/settings/page.tsx`, `src/components/security-settings.tsx`.
- **P3(d)** 🟢 — Payment Methods "Crypto (SOL, USDC) — SOON" → **"Active"** (SOL/USDC checkout works). File: `src/app/settings/page.tsx`.

## Retest queue (🟢 awaiting Judah)
S1 · L1 · S2 · L2 · P2 · POL2 · POL4 · P3(c) · P3(d)  — pull the latest deploy and retest these.

## Done (✅ verified by Judah)
_(empty)_

## Notes / decisions needed from Judah
- **B2**: Coinbase Commerce vs. SOL-as-crypto-rail — which?
- **Blocked-on-you (flip these, I'll prep the code):** A2 Privy social providers · A4 Privy Solana embedded-wallet toggle · A5/A6 Resend domain verify · B1 enable Moov sandbox.
- **PASS items I must not break** (flag if a fix risks them): log-in/out session restore, onboarding screens, unlink wallet, wallet balance, filters/sort, listing detail render, mint, delist, fee math, double-click-buy dedupe.
