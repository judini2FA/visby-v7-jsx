// Override to 'http://localhost:3000' for local dev.
// The app is served at visby.me (app.visby.me does not resolve). If the app ever moves to a subdomain,
// update this AND background.js's VISBY_ORIGIN_BG AND the manifest's externally_connectable matches.
const VISBY_ORIGIN = 'https://visby.me';
