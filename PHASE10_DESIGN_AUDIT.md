# Phase 10 — Design Audit (vs design.md)

Generated by a 15-route fan-out audit, each finding adversarially re-verified to drop false positives (brand hexes, functional colors, the frozen homepage logo, price() sizes).

**119 verified violations** — 12 high, 60 medium, 47 low, across 29 files.

Routes clean: /marketplace, /login. Biggest offenders: checkout-modal, header-menu, profile.

> **checkout-modal.tsx (4 high) is deferred** — it is the money-path buy flow; its type-scale/spacing/glass rewrite needs a careful isolated pass with checkout testing, not a bulk edit.

---

## `src/components/checkout-modal.tsx`  (4H 2M )

- **[HIGH] SYSTEM BYPASS — helpers not used (rules 1,2,3,4,5)** (line 351)  
  The checkout bottom sheet (opened directly by the /item/[id] Buy flow) hand-rolls the glass sheet instead of sheet(). borderRadius '30px' is off the radius tokens (--r-xl is 28px) and padding '24px 20px 36px' mixes off-grid values (20 and 36 are not on the S g  
  _Fix:_ Compose from sheet() for the surface, tokenized radius (var(--r-xl)) and S-grid padding (e.g. `${S[5]}px ${S[5]}px ${S[7]}px`).
- **[HIGH] TYPE SCALE (rule 2) — pervasive raw fontSizes + Quicksand on UI text** (line 358)  
  The checkout modal never uses t(). Raw off-scale fontSizes (18 here, plus 22/16/15/14/13/12/11/10/9/7 across the file) drive every text node, and Quicksand is applied to body/label/UI text — design.md §3 reserves Quicksand for wordmark/title/display and mandat  
  _Fix:_ Replace every raw fontSize with a t() role (title/heading/body/meta/micro), use price() for the 'You pay' amount, and drop the Quicksand overrides on non-title UI text.
- **[HIGH] SPACING GRID (rule 3) — pervasive off-grid values** (line 369)  
  The price-summary panel and its siblings use off-grid spacing throughout: padding '14px 16px', marginBottom 20, borderRadius 18, plus gaps/margins of 10, 6, 9, 3 elsewhere (lines 369, 392, 396, 410, 443, 537, 549, 572, 610). None of 14/20/18/10/9/6/3 are on S   
  _Fix:_ Snap every padding/margin/gap to S (e.g. marginBottom: S[5], padding: `${S[3]}px ${S[4]}px`, gap: S[3]) and radius to a --r* token. Reuse surface() for these inset panels so radius/border/spacing come from the system.
- **[HIGH] GLASS-NESTING LAW (rule 4)** (line 369)  
  The modal's outer surface is glass (--glass-bg-strong, line 351). The price-summary, ship-to, and quote/panel blocks nested inside it are all filled with var(--glass-bg) — glass inside glass. Per §7 nested panels must be near-solid surface() with no compounded  
  _Fix:_ Convert every inner panel (lines 369, 410, 512, 537, 572, 598, 610) to surface() so they read as quiet inset panels with a hairline border.
- **[MED] GRADIENT/BUTTON SYSTEM (rule 5, design.md §8) + TOKENS (rule 1)** (line 99)  
  Every primary/pay CTA (lines 99, 428, 473, 518, 524, 562, 586, 604, 626) hand-rolls the local GH gradient with a hardcoded color:'#fff' instead of btn('primary'). '#fff' is a hardcoded theme color that should be var(--text-on-cta), and the bespoke buttons bypa  
  _Fix:_ Use btn('primary', { full: true }) for all pay/confirm/add-funds CTAs and remove the local GH constant and every color:'#fff'.
- **[MED] TAB SYSTEM (design.md §8) + GLASS-NESTING (rule 4)** (line 443)  
  The currency selector is a bespoke segmented control (glass-filled, off-grid gap:6/marginBottom:20) instead of tabSlider(). design.md §8 says tabs must always use tabSlider(); its glass fill nested in the glass modal also breaks the nesting law.  
  _Fix:_ Rebuild the currency selector with tabSlider() (wrap/item/itemActive) — its wrap is surface()-based (no blur) and its active item carries the brand gradient.

## `src/components/layout/header-menu.tsx`  (2H 6M 6L)

- **[HIGH] TOKENS ONLY (hardcoded theme scrim in imported component)** (line 74)  
  HeaderMenu is rendered in the profile header (page.tsx line 505). Its bottom-sheet backdrop hardcodes rgba(0,0,0,.5) instead of the --modal-scrim token, so the overlay is pure black in light mode rather than the plum-tinted scrim.  
  _Fix:_ Replace background: 'rgba(0,0,0,.5)' with background: 'var(--modal-scrim)'.
- **[HIGH] GLASS-NESTING LAW — glass inside glass** (line 60)  
  The menu trigger button is a backdrop-blurred frosted-glass panel (--glass-bg + blur(var(--glass-blur))) rendered via <HeaderMenu/> at page.tsx:161, which sits inside the page's sticky glass header (page.tsx:155 is itself --glass-bg-strong + backdropFilter blu  
  _Fix:_ Render the trigger with surface() (near-solid, no blur) instead of hand-rolled --glass-bg + backdropFilter, or use btn('secondary')/btn('text') so it doesn't stack blur on the header glass.
- **[MED] TOKENS ONLY (hardcoded theme color)** (line 74)  
  The bottom-sheet backdrop scrim hardcodes rgba(0,0,0,.5) as a surface color instead of a theme token, so it won't adapt across light/dark. tokens.css exposes a modal/sheet scrim variable for exactly this.  
  _Fix:_ Use a scrim token (e.g. background: 'var(--modal-scrim)') instead of the literal rgba(0,0,0,.5).
- **[MED] SPACING GRID (off-grid padding + raw radius)** (line 63)  
  The hamburger menu button uses padding: '9px 11px' (both off the 4/8 grid) and a raw borderRadius: 14 instead of a --r* token.  
  _Fix:_ Use padding: '8px 12px' (S[2]/S[3]) and borderRadius: 'var(--r-sm)' for the icon button.
- **[MED] TOKENS ONLY (hardcoded theme color)** (line 74)  
  Verified: line 74 hardcodes rgba(0,0,0,.5) as a full-screen bottom-sheet scrim surface. The rubric explicitly lists rgba(0,0,0,.x) used as a surface as a TOKENS-ONLY violation. The codebase already has the token for this exact purpose: tokens.css line 64 defin  
  _Fix:_ Replace background: 'rgba(0,0,0,.5)' with background: 'var(--img-scrim)' so the scrim comes from the token and matches the DisputePanel overlay.
- **[MED] SPACING GRID — off-grid radius/padding** (line 63)  
  borderRadius 14 is off the radius token scale and padding '9px 11px' is off the S grid (S = 4/8/12/16/24…); 9 and 11 are hand-rolled numbers the design system bans.  
  _Fix:_ Use a radius token (borderRadius: 'var(--r-sm)') and grid padding (padding: `${S[2]}px ${S[3]}px`, i.e. '8px 12px'), or adopt surface()/btn() which already carry token radius and grid padding.
- **[MED] 1 — Tokens only (hardcoded theme color)** (line 74)  
  The bottom-sheet backdrop dim hardcodes rgba(0,0,0,.5) as a surface. tokens.css already defines a theme-aware --modal-scrim (rgba(15,12,24,.45) in light, rgba(0,0,0,.65) in dark) for exactly this. The literal value ignores the mode-specific scrim and won't tra  
  _Fix:_ Use the token: background: 'var(--modal-scrim)'.
- **[MED] 3/5 — Spacing grid + Radius token** (line 63)  
  The hamburger menu button hand-rolls an off-system chip: borderRadius 14 is not one of the radius tokens (--r-sm 12 / --r 16 / --r-lg 20 / --r-xl 28 / --pill 999), and padding '9px 11px' is off the 4/8 spacing grid (S = 4/8/12/16/24/32/48/64) — neither 9 nor 1  
  _Fix:_ Set borderRadius: 'var(--r)' (16) or 'var(--r-sm)' (12), and put padding on-grid, e.g. padding: `${S[2]}px ${S[3]}px` (8px 12px). Better: build the button from btn('secondary', { pill: false }) so surface, border, radius and blur all come f
- **[LOW] 1 — Tokens only (modal scrim color)** (line 74)  
  The header menu bottom-sheet backdrop hardcodes rgba(0,0,0,.5) instead of the --modal-scrim token the onboarding sheet uses — same scrim rendered with two different literal values across the app, and it won't flip per mode.  
  _Fix:_ Replace with 'var(--modal-scrim)' to unify the modal scrim across the filters sheet, header menu, and onboarding.
- **[LOW] 5 — Radius scale (off-token radius) + off-grid padding** (line 63)  
  The hamburger menu button uses borderRadius:14 (off the radius scale — --r-sm is 12, --r is 16) and padding '9px 11px' (9 and 11 are off the S grid).  
  _Fix:_ Use a token radius (borderRadius: 'var(--r-sm)' = 12) and grid padding (e.g. padding: `${S[2]}px ${S[3]}px`).
- **[LOW] 3 — Spacing grid (off-grid icon box size)** (line 81)  
  The menu-row icon boxes are 42x42 — off the S grid (nearest grid dimensions are 40 or 48). Repeated for every link row at line 92. surface() is used correctly (no nesting-law violation), but the fixed dimension is off-grid.  
  _Fix:_ Snap the icon box to a grid dimension — 40 or 48 — for consistency with the 4/8 grid.
- **[LOW] TOKENS ONLY (hardcoded rgba(0,0,0) scrim)** (line 74)  
  The bottom-sheet backdrop scrim is a literal rgba(0,0,0,.5). tokens.css defines --modal-scrim (light rgba(15,12,24,.45) / dark rgba(0,0,0,.65)) exactly for modal backdrops, so overlay opacity stays theme-aware. (Note: --scrim is near-transparent and is NOT the  
  _Fix:_ Use background: 'var(--modal-scrim)' for the overlay instead of the literal rgba(0,0,0,.5).
- **[LOW] TOKENS ONLY (hardcoded theme scrim)** (line 74)  
  The HeaderMenu bottom-sheet backdrop (rendered in this route's header) uses a literal rgba(0,0,0,.5) scrim. The design system provides --img-scrim (rgba(0,0,0,.55)) for exactly this — the ReportButton modal on the same page uses var(--img-scrim) (report-button  
  _Fix:_ Use background: 'var(--img-scrim)' for the modal backdrop, matching the ReportButton sheet overlay.
- **[LOW] TOKENS ONLY — hardcoded theme scrim** (line 74)  
  The modal backdrop dim is a hardcoded rgba(0,0,0,.5) theme color rather than a token, so it won't respond to theme/reduced-transparency settings. A dedicated --modal-scrim token already exists (light rgba(15,12,24,.45), dark rgba(0,0,0,.65)) for exactly this o  
  _Fix:_ Use the overlay token: background: 'var(--modal-scrim)' instead of a literal rgba black.

## `src/app/profile/page.tsx`  (2H 3M 2L)

- **[HIGH] TOKENS ONLY (hardcoded theme scrim)** (line 154)  
  The transfer-modal backdrop hardcodes a pure-black scrim. tokens.css defines a dedicated modal scrim (--modal-scrim: rgba(15,12,24,.45) light / rgba(0,0,0,.65) dark). rgba(0,0,0,.5) does not flip with theme, so light mode gets a too-heavy pure-black overlay in  
  _Fix:_ Replace background: 'rgba(0,0,0,.5)' with background: 'var(--modal-scrim)'.
- **[HIGH] GRADIENT BUDGET + TOKENS ONLY (non-brand gradient + hardcoded #fff on a nested chip)** (line 169)  
  A gradient is painted on a plain nested chip inside a surface() row inside a sheet(), which is not an allowed gradient target (CTA / price / active tab / avatar / story ring / background / text-clip). #14F195 and #9945FF are Solana brand hexes, NOT part of the  
  _Fix:_ Use avatar('sm') filled with the brand gradient (or surface()) for the icon box, drop the Solana hexes, and use color: 'var(--text-on-cta)' / 'var(--text-strong)' instead of literal #fff.
- **[MED] TYPE SCALE (raw off-scale fontSize)** (line 169)  
  fontSize: 9 is below the type scale (smallest role is micro=11) and is not price(). Raw off-scale sizes are disallowed.  
  _Fix:_ Use ...t('micro') for the 'SOL' label (or badge() if it should read as a chip).
- **[MED] TYPE SCALE (raw off-scale fontSize overriding t())** (line 559)  
  This spreads t('micro') (11px) then overrides it to fontSize: 9.5 (a fractional, off-scale size) and rewrites letterSpacing, defeating the single type scale for the stat labels.  
  _Fix:_ Drop the fontSize/letterSpacing overrides and keep ...t('micro') as-is; if the labels overflow at 11px, shorten the labels rather than shrinking the type off-scale.
- **[MED] TOKENS ONLY (hardcoded theme shadow)** (line 502)  
  The sticky header hardcodes a black-based drop shadow. Theme shadows come from tokens (--box-shadow-soft is none in dark mode). rgba(0,0,0,.06) does not adapt to dark mode and re-implements chrome elevation by hand.  
  _Fix:_ Use boxShadow: 'var(--box-shadow-soft)' (or 'var(--glass-shadow)') so header elevation flips correctly with the theme.
- **[LOW] TYPE SCALE (raw off-scale fontSize)** (line 377)  
  The '@' username prefix uses fontSize: 15, which is not a t() type-scale role (roles are 11/12/14/16/22/30). input() uses 15 internally, but this prefix is a standalone text node and should map to a scale role.  
  _Fix:_ Use ...t('body') (14) for the '@' prefix so it aligns with the input's text instead of a raw 15.
- **[LOW] SPACING GRID (off-grid padding) + border width off-token** (line 168)  
  pad: '12px 14px' — the 14 is off the S grid (4/8/12/16/24...). The selected-state border uses an arbitrary 1.5px width; borders should come from the standard 1px hairline, with selection shown by fill/token rather than a hand-picked width.  
  _Fix:_ Use surface({ pad: '12px 16px' }) (both on-grid). For the selected state prefer a 1px token-color border or a surface fill change rather than 1.5px.

## `src/app/buy-crypto/page.tsx`  (1H 4M )

- **[HIGH] Rule 1 — Tokens only (invalid CSS from hex-suffixing a var())** (line 548)  
  C.green is defined as 'var(--ok)' (page.tsx line 28), so `${C.green}12` produces the string `var(--ok)12` — an invalid CSS color the browser drops entirely. The success card's fill and border silently fail to render and it bypasses the success surface system.   
  _Fix:_ Stop concatenating alpha onto a var(). Use the success helper/tokens the system provides: badge('success')/surface() with the --ok-soft token for the tinted fill and border (background:'var(--ok-soft)', border:'1px solid var(--ok-soft)', ic
- **[MED] Rule 1 — Tokens only (hardcoded theme shadow)** (line 476)  
  The sticky glass header hardcodes a black-alpha shadow that won't flip with light/dark and hand-duplicates the glass shadow system. Glass surfaces must use the tokenized shadow so they read correctly in dark mode.  
  _Fix:_ Use the glass shadow token: boxShadow: 'var(--glass-shadow)'. Better, build the header bar from glass()/sheet() so border, blur, and shadow all come from the system.
- **[MED] Rule 2 — Type scale (raw off-scale fontSize)** (line 615)  
  The SOL/USDC token-picker buttons set a raw fontSize:15, off the t() scale (30/22/16/14/12/11). The scale forbids raw fontSize outside t()/price().  
  _Fix:_ Rebuild as the segmented control via tabSlider(), whose item style carries the correct font (13). If kept as buttons, use btn() (fontSize 14) or spread t('body') — never a raw 15.
- **[MED] Rule 3 — Spacing grid (off-grid padding)** (line 612)  
  11 and 18 are not on the S grid (4/8/12/16/24/32/48/64). All padding must come from S; these token-picker buttons use arbitrary pixel pad.  
  _Fix:_ Use grid values — e.g. padding: `${S[3]}px ${S[4]}px` (12/16) — or drop this bespoke control and use tabSlider()/btn(), which already pad on-grid.
- **[MED] Rule 6 / design.md — bespoke segmented control instead of tabSlider()** (line 602)  
  A two-option segmented picker hand-rolled with pill + gradient-active styling. design.md mandates all segmented/tab selectors use tabSlider() rather than a bespoke control. Hand-rolling it produced the off-scale fontSize (615) and off-grid padding (612) above.  
  _Fix:_ Replace with tabSlider(): spread wrap on the container and item/itemActive on each option. That removes the raw fontSize:15, the '11px 18px' padding, and the manual gradient in one move.

## `src/components/pay-request.tsx`  (1H 3M 4L)

- **[HIGH] Component anatomy — tabs must use tabSlider() (design.md §8/§9); GRADIENT/GLOW budget; TYPE SCALE** (line 379)  
  Confirmed lines 379-410: a bespoke Pay|Request segmented control hand-rolled inline. design.md §8 ('never a bespoke segmented control') and §9 mandate tabSlider(). It also hardcodes a black theme shadow rgba(0,0,0,.18) on the sliding gradient pill (won't flip   
  _Fix:_ Replace the block with tabSlider(): render {wrap} and map the two modes to {item}/{itemActive}. This drops the hand-rolled gradient pill, the rgba(0,0,0,.18) shadow, and the fontSize:15.
- **[MED] TYPE SCALE** (line 418)  
  Confirmed at line 418 (symbol span) and repeated verbatim on the amount input at L444. Raw fontSize 48 is off the type scale and larger than price()'s max (lg = 30). The gradient-clip treatment is hand-rolled here rather than routed through price().  
  _Fix:_ Route the hero amount through the system: `price('lg')` already provides the gradient-clip treatment this code hand-rolls, or `...t('display')` (30). If a larger keypad numeral is genuinely sanctioned it should be added to price() as a name
- **[MED] TYPE SCALE** (line 689)  
  Confirmed at line 689. The confirm-sheet amount uses raw fontSize 32, which is off the type scale (nearest roles: title 22, display 30) and is not price().  
  _Fix:_ Use `...t('display')` (30) for the confirmation amount, or price('lg') if it should read as a price. Do not inline 32.
- **[MED] TYPE SCALE (design.md §3) — off-scale raw fontSize (48 entry / 32 confirm); only price() may be off-scale** (line 418)  
  Confirmed: the hero amount entry uses raw fontSize:48 on both the symbol span (L418) and the input (L444), and the confirm sheet uses raw fontSize:32 (L689). All three are off the t() scale (display caps at 30) and not routed through price() (max lg=30), which  
  _Fix:_ Cap the amount at t('display')/price('lg') (30), or add a dedicated hero scale entry if a larger numeral is genuinely needed. At minimum replace the confirm-sheet 32 with price('lg') and stop hand-rolling 48.
- **[LOW] TYPE SCALE** (line 402)  
  Confirmed at line 402. The Pay|Request control uses raw fontSize 15 (off-scale) and off-grid vertical padding '10px 0' (10 not on S grid). Raw fontSize 15 recurs on the recipient picker (L527) and note input (L544). NOTE: design.md §8's tabSlider mandate names  
  _Fix:_ For the recipient/note inputs use input() (the helper sets fontSize 15 centrally). For the segmented control, prefer tabSlider() (fontSize 13, pill radius, gradient active state, on-grid padding) so the type and gradient budget stay central
- **[LOW] SPACING GRID** (line 474)  
  Confirmed at line 474. The SOL/USDC toggle padding '7px 18px' is off the S grid (7 and 18 are not grid values). It is also a bespoke gradient pill duplicating the active-tab pattern tabSlider() owns.  
  _Fix:_ Use an on-grid pad (e.g. '8px 16px' = S[2]/S[4]) or express the toggle via tabSlider() for the active-gradient state so the gradient budget stays centralized.
- **[LOW] SPACING GRID (design.md §4) — every padding from S = {4,8,12,16,24,32,48,64}** (line 474)  
  Confirmed line 474: the SOL/USDC token toggle is a hand-rolled pill with off-grid padding '7px 18px' (7 and 18 are not on the S grid). It also duplicates a fifth button style instead of using btn().  
  _Fix:_ Render these as btn('primary'/'secondary') pills, or if a compact chip is needed use on-grid padding like '8px 16px' (S[2]/S[4]).
- **[LOW] TOKENS ONLY (design.md §6) — hardcoded rgba(0,0,0,.5) modal scrims repeated in three places** (line 657)  
  Confirmed literal rgba(0,0,0,.5) at pay-request.tsx L657 and L815, and layout/header-menu.tsx L74. A theme black used as a surface, repeated in three places with no shared token; there is no overlay-scrim token (only --img-scrim for photo badges and --scrim wh  
  _Fix:_ Introduce a scrim token (e.g. var(--overlay-scrim)) and reference it in all three overlays instead of the literal rgba(0,0,0,.5).

## `src/components/payment-methods-manager.tsx`  (1H 1M 1L)

- **[HIGH] GLASS-NESTING LAW (design.md §7) — no glass inside glass; nested panels use surface()** (line 132)  
  Confirmed line 132: the method-options dropdown spreads surface() then overrides it back into real frosted glass (glass-bg-strong + glass-border + glass-shadow). It renders inside the MethodCard, which itself sits inside the wallet page's tab content — glass s  
  _Fix:_ Use ...surface({ pad: '6px' }) alone (near-solid var(--surface-bg), hairline border, box-shadow-soft, no blur). Remove the background/border/boxShadow overrides.
- **[MED] TYPE SCALE (design.md §3) — font sizes come from t(); only price() may be off-scale** (line 119)  
  Confirmed line 119: the method balance ('SOL 3.520') is a hand-rolled fontSize:22/fontWeight:800 numeral not from t() and not price(); when isDefault it also applies gradient text-clip — a price display bypassing the one sanctioned off-scale path. price('md')   
  _Fix:_ Use price('md') for the amount; use t('title')/t('heading') for the non-default greyscale case. Do not hand-roll fontSize:22.
- **[LOW] SPACING GRID (design.md §4) — off-grid padding on the 'primary'/'destination' slot tiles** (line 167)  
  Confirmed line 167 ('26px 4px 4px') and tally-wallets.tsx line 159 ('20px 3px 3px'): the gradient PRIMARY/DESTINATION slot tiles use off-grid padding (26, 20, 3 are not on the S grid). §4: 'If a value isn't on the grid, it's wrong.'  
  _Fix:_ Snap to the grid: e.g. padding: `${S[6]}px ${S[1]}px ${S[1]}px` (24/4/4), and do the same for the tally-wallets slot.

## `src/components/tally-wallets.tsx`  (1H 1M )

- **[HIGH] GLASS-NESTING LAW (design.md §7) — no glass inside glass; nested panels use surface()** (line 149)  
  Confirmed line 149: same pattern — the wallet-options dropdown spreads surface() then overrides it back into frosted glass. TallyWallets is rendered inside a surface() block at wallet/page.tsx line 90-91, so this is glass nested inside a nested panel — violate  
  _Fix:_ Use ...surface({ pad: '6px' }) with no overrides (remove the background/border/boxShadow lines) so the menu is a quiet near-solid inset panel, not stacked glass.
- **[MED] GRADIENT BUDGET / COLOUR PALETTE (design.md §6) — no third/fourth accent colour system; UI colour comes from -** (line 13)  
  Confirmed lines 13-17, used at line 40. These are three bespoke multi-stop gradients (Solana green→purple, Ethereum blue→slate, Bitcoin orange) — NONE are on the allowed brand-accent list and none are functional colors. They put three off-brand accent colour s  
  _Fix:_ Paint ChainBadge with var(--grad-brand) (or a neutral surface() chip with the SOL/ETH/BTC abbreviation in --text-strong) and distinguish chains by the label text rather than bespoke brand gradients.

## `src/components/security-settings.tsx`  (6M 3L)

- **[MED] TYPE SCALE (off-scale fontSize)** (line 294)  
  btn() sets an on-scale fontSize 14. Overriding with raw fontSize: 12 on the 'Forgot password?' button is off the t() scale; design.md line 46 forbids raw fontSize outside the scale (price excepted) and line 123 explicitly maps off-scale fontSize -> t('meta').  
  _Fix:_ Drop the numeric override and spread ...t('meta') for the label sizing (12px with the scale's family/weight).
- **[MED] TYPE SCALE (off-scale fontSize)** (line 312)  
  btn() already supplies fontSize 14. The raw fontSize: 13 override is off the t() scale; design.md line 123 flags exactly this ('fontSize: 13 (off-scale)' -> '...t('meta')').  
  _Fix:_ Remove fontSize: 13 so btn('primary') keeps its 14px, or spread ...t('meta') if a smaller label is truly needed.
- **[MED] TYPE SCALE (off-scale fontSize)** (line 316)  
  Raw fontSize: 13 overrides btn('secondary')'s on-scale 14; 13 is not a t() role (design.md line 123).  
  _Fix:_ Remove fontSize: 13 so btn('secondary') keeps its scale value.
- **[MED] TYPE SCALE (off-scale fontSize)** (line 358)  
  Raw fontSize: 12 overrides btn()'s 14 for the per-session 'Log out' action; off the t() scale.  
  _Fix:_ Spread ...t('meta') instead of the raw fontSize: 12 (keep color: 'var(--danger)').
- **[MED] TYPE SCALE (off-scale fontSize)** (line 367)  
  Raw fontSize: 13 on 'Log out all other devices' overrides the button's on-scale 14; 13 is not a t() role.  
  _Fix:_ Remove fontSize: 13 and keep btn('secondary')'s 14px.
- **[MED] TYPE SCALE (off-scale fontSize)** (line 399)  
  Raw fontSize: 13 override on the Delete-account confirm button is off the t() scale (btn sets 14).  
  _Fix:_ Remove fontSize: 13. (Separately, painting btn('primary') with a --danger background bypasses the four-variant system — prefer btn('danger', { full: true }); the --danger color itself is functional and not a token violation.)
- **[LOW] TYPE SCALE (off-scale fontSize)** (line 403)  
  Raw fontSize: 13 override, off the t() scale (btn sets 14).  
  _Fix:_ Remove the fontSize: 13 override.
- **[LOW] SPACING GRID (off-grid padding)** (line 290)  
  The 14px bottom padding is off the S grid (4 and 16 are on-grid; 14 is not). The same '4px 16px 14px' block padding repeats on lines 342 and 389.  
  _Fix:_ Round to the grid: padding: `${S[1]}px ${S[4]}px ${S[4]}px` (4px 16px 16px).
- **[LOW] TOKENS ONLY (hardcoded #fff surface)** (line 35)  
  Same literal #fff knob fill as the settings-page Toggle, duplicated here. Same borderline status (on-state sits on the gradient). Same correction: the claim's proposed var(--text-on-cta) is a dark color, not white.  
  _Fix:_ Extract one shared Toggle component driven by a light knob token; do not use var(--text-on-cta) (dark). Keep the fill mode-independent.

## `src/app/dashboard/seller/page.tsx`  (4M 5L)

- **[MED] TYPE SCALE (raw fontSize off-scale)** (line 210)  
  The photo-remove '×' button sets a raw fontSize: 13, which is off the type scale (allowed sizes are 30/22/16/14/12/11). (C.red resolves to var(--danger) and the on-color '#fff' glyph are acceptable; the off-scale fontSize is the violation.)  
  _Fix:_ Drop fontSize:13 and use ...t('meta') (12) for the glyph, or replace the scaled text glyph with an inline SVG close icon.
- **[MED] TYPE SCALE (raw fontSize off-scale)** (line 385)  
  The relist edit-cancel '×' button overrides the button font with a raw fontSize: 20, which is not on the type scale.  
  _Fix:_ Remove the fontSize:20 override and let btn('text') carry its own size, or use an inline SVG close icon instead of a scaled text character.
- **[MED] SPACING GRID (off-grid padding)** (line 246)  
  Category chips use padding: '7px 16px' — the 7px vertical is off the 4/8 spacing grid (S = 4,8,12,16,24,32,48,64).  
  _Fix:_ Use padding: '8px 16px' (S[2]/S[4]) or let btn() supply its own '12px 20px' padding.
- **[MED] SPACING GRID (off-grid padding)** (line 434)  
  The three relist stat tiles use pad: '12px 10px' — the 10px horizontal is off the spacing grid.  
  _Fix:_ Use pad: '12px 8px' (S[3]/S[2]) so both axes land on the grid.
- **[LOW] TOKENS ONLY (hardcoded shadow)** (line 732)  
  The sticky Sell header is a top-level glass surface (glass-bg-strong + blur) but hardcodes its drop shadow with rgba(0,0,0,.06) instead of the canonical glass shadow token, so it won't flip with theme and diverges from the glass system.  
  _Fix:_ Replace with boxShadow: 'var(--glass-shadow)' so the header borrows the shared glass shadow rather than a bespoke rgba value.
- **[LOW] SPACING GRID (off-grid padding)** (line 352)  
  The 'Lists as Used' note surface uses pad: '6px 10px' — both 6 and 10 are off the 4/8 grid.  
  _Fix:_ Use pad: '8px 12px' (S[2]/S[3]) — nearest grid values.
- **[LOW] SPACING GRID (off-grid padding)** (line 377)  
  The relist edit-price input uses padding: '10px 44px 10px 28px'; the 10px vertical is off-grid (44/28 are affordance offsets for the $ and USDC adornments).  
  _Fix:_ Set the vertical padding to a grid value (e.g. '12px 44px 12px 28px', S[3]) and keep the horizontal adornment offsets.
- **[LOW] SPACING GRID (off-grid padding)** (line 681)  
  The bulk-log price input uses 10px vertical padding, off the 4/8 grid (12 and 24 are on-grid).  
  _Fix:_ Use '12px 12px 12px 24px' (S[3]/S[5]) so every side is on the grid.
- **[LOW] TOKENS ONLY / DUPLICATED COMPONENT (bespoke toggle)** (line 269)  
  This 'List for Sale Now' switch is a second, bespoke toggle whose ON state is var(--text-strong) with a hardcoded #fff knob and rgba(0,0,0,.3) shadow — while the same file's canonical Toggle component (line 466) uses var(--grad-brand) ON and var(--text-muted)   
  _Fix:_ Reuse the existing Toggle component (lines 466-473) here so both switches match and the knob/shadow use tokens (or gradient ON) rather than #fff / rgba(0,0,0,.3).

## `src/app/mint/page.tsx`  (3M 3L)

- **[MED] 1 — Tokens only (no hardcoded theme colors)** (line 197)  
  The sticky header is a glass surface but hardcodes a black-based drop shadow rgba(0,0,0,.06). Black shadows are theme colors — they don't flip for light/dark and read wrong against the dark-mode glass. Verified: --glass-shadow exists and is defined per-theme i  
  _Fix:_ Use boxShadow: 'var(--glass-shadow)' (the tokenized, theme-flipping shadow the glass()/card() helpers already use), or build the header off glass()/card() so it inherits it.
- **[MED] 7 — No emojis/glyph icons (inline SVG only) + 1 — hardcoded #fff + 2 — off-scale fontSize** (line 239)  
  The photo remove button renders a raw '×' (U+00D7) text glyph as its icon — design.md mandates inline SVG icons only, a glyph is not an SVG. It also hardcodes color: '#fff' (a theme color, rule 1) and fontSize: 12 as a raw numeric off the type scale (rule 2).   
  _Fix:_ Replace the '×' with an inline SVG close icon (two crossed <line>s) matching the other SVGs in this file; use stroke='#fff' on the scrim-backed button (the danger-red circle is an always-solid backdrop) and drop the raw fontSize since SVG s
- **[MED] 3 — Spacing grid + 8 — use the badge() helper instead of a hand-rolled pill** (line 223)  
  The 'Cover' badge is hand-rolled with off-grid spacing (padding '1px 5px' — neither 1 nor 5 is on the S grid {4,8,12,16,24,32,48,64}) and an off-token radius (4, no matching --r* token; --r-sm is 12). It duplicates the existing badge() component; a badge sitti  
  _Fix:_ Replace the inline object with badge('onImage') (keeping the absolute-position props). That fixes radius, padding, and scrim in one move and matches the listing-card badge.
- **[LOW] 1 — Tokens only (no hardcoded theme colors)** (line 305)  
  The List-for-Sale toggle knob hardcodes background: '#fff'. Unlike the on-scrim spinner, this knob sits on the toggle track in both themes and is not backed by a guaranteed-dark scrim, so a bare '#fff' is a hardcoded theme color that won't flip (rule 1). (The   
  _Fix:_ Use a token for the knob fill that stays legible on both track states/themes — e.g. background: 'var(--surface-bg)' or an on-cta/on-glass token — instead of a bare '#fff'.
- **[LOW] 1 — Tokens only (no hardcoded theme colors)** (line 232)  
  The remove-background toggle button hardcodes a black-based shadow rgba(0,0,0,.2) — a theme shadow that won't flip for light/dark (rule 1). Verified --box-shadow-soft exists as the tokenized equivalent (and correctly goes to 'none' in dark mode).  
  _Fix:_ Use boxShadow: 'var(--box-shadow-soft)' (the token surface()/input() already use) instead of the hand-rolled black shadow.
- **[LOW] 2 — Radius token (consume via var(--r*))** (line 222)  
  The photo preview <img> (line 222) and its busy overlay (line 225) use raw borderRadius: 12 instead of the radius token. 12 is exactly --r-sm; consuming radii via the var(--r*) tokens keeps corners in sync with the scale rather than hand-rolled numbers.  
  _Fix:_ Use borderRadius: 'var(--r-sm)' on both the <img> (line 222) and the busy overlay (line 225).

## `src/app/settings/page.tsx`  (3M 3L)

- **[MED] SPACING GRID (off-grid padding)** (line 95)  
  The back button uses '6px 10px' — neither 6 nor 10 is on the S grid {4,8,12,16,24,32,48,64}; btn() already supplies 12px 20px.  
  _Fix:_ Use on-grid padding, e.g. `${S[2]}px ${S[3]}px` (8px 12px), or drop the override to keep btn()'s 12px 20px.
- **[MED] SPACING GRID (off-grid padding)** (line 123)  
  7 and 14 are not on the S grid. btn() already supplies the sanctioned 12px 20px; the '7px 14px' override introduces off-grid spacing. The same pattern repeats on line 124 (secondary currency chip) and lines 175/188.  
  _Fix:_ Drop the padding override, or if denser chips are needed use an on-grid value like `${S[2]}px ${S[3]}px` (8px 12px).
- **[MED] SPACING GRID (off-grid padding)** (line 175)  
  '7px 14px' is off-grid (7 and 14 not in S). The same off-grid button padding recurs across security-settings.tsx (lines 281, 287, 324, 338, 378, 386 all use '7px 14px').  
  _Fix:_ Remove the padding overrides so btn() keeps 12px 20px, or standardize a compact button on an S-grid value (8px 12px) rather than 7/14.
- **[LOW] SPACING GRID (off-grid padding)** (line 133)  
  14px vertical padding is off the S grid; both the Shipping and Address Book section bodies (lines 133 and 142) use '14px 16px'.  
  _Fix:_ Use S[4] both axes ('16px 16px') or `${S[3]}px ${S[4]}px` (12px 16px) to match the Row list-item padding.
- **[LOW] TOKENS ONLY (hardcoded rgba(0,0,0) shadow)** (line 93)  
  The sticky header hand-rolls its glass surface with a literal rgba(0,0,0,.06) shadow instead of the --glass-shadow token. A hardcoded black shadow does not adapt to light/dark. (The header is legit floating chrome, not glass-nested-in-glass, so only the litera  
  _Fix:_ Swap the literal for boxShadow: 'var(--glass-shadow)' (or build the header from glass()) so it flips with the theme.
- **[LOW] TOKENS ONLY (hardcoded #fff surface)** (line 85)  
  The toggle knob fill is a literal #fff surface color (rule 1: surfaces should be greyscale tokens so they flip with the theme). Borderline: when on, the knob sits on the brand gradient, so a mode-independent light fill is defensible (mirrors avatar()'s #fff-on  
  _Fix:_ Introduce/use a dedicated light knob token (a mode-independent near-white) rather than a bare '#fff'; do NOT use var(--text-on-cta) (it is dark). If a single token is added, share it with the duplicate Toggle in security-settings.tsx line 3

## `src/app/p/[wallet]/page.tsx`  (3M 3L)

- **[MED] TOKENS ONLY (hardcoded theme color)** (line 141)  
  The sticky header (already glass: var(--glass-bg-strong) + blur) hardcodes a black rgba shadow as a theme color. It won't flip for light/dark and duplicates the token system's ambient shadow (--glass-shadow, which is defined per-theme in tokens.css).  
  _Fix:_ Replace with the token shadow: boxShadow: 'var(--glass-shadow)'. Drop the hand-rolled rgba.
- **[MED] TYPE SCALE (raw off-scale fontSize) + avatar-size override** (line 165)  
  avatar('lg') is 64px with fontSize 22 (AVATAR_SIZE/AVATAR_FS in ui.ts lines 215-216). This inline-overrides it to width/height 80 and fontSize 28 — an off-scale size and an off-scale font size (28 is not a t() role; neighbors are 30 'display' and 22 'title').  
  _Fix:_ Use avatar('lg') as-is (64px, fontSize 22) without the width/height/fontSize overrides. If an 80px hero avatar is intentional, add a real 'xl' size to the avatar() helper in src/lib/ui.ts rather than overriding inline, and let AVATAR_FS sup
- **[MED] TYPE SCALE (raw off-scale fontSize)** (line 223)  
  The stat label spreads t('micro') (11px, 0.06em) then overrides fontSize to 9 and letterSpacing to 0.01em — a raw font size below the smallest type role (micro at 11 is the floor).  
  _Fix:_ Use t('micro') as-is (11px) for the stat labels, or t('meta') if a slightly larger label reads better. Remove the fontSize: 9 / letterSpacing override — nothing should render text below the micro role.
- **[LOW] RADIUS (off-token radius)** (line 40)  
  surface() in the SoldRow thumbnail is passed literal radius: 14, off the radius token scale (--r-sm 12 / --r 16 / --r-lg 20 / --r-xl 28).  
  _Fix:_ surface({ radius: 'var(--r-sm)' }) (12px, the documented nested-surface radius) instead of the literal 14.
- **[LOW] SPACING GRID (off-grid + hardcoded padding)** (line 161)  
  The profile hero top padding 28px is off the S grid (nearest are S[5]=24 and S[6]=32); the bottom 24 is on-grid but hardcoded as a literal instead of S[5].  
  _Fix:_ Use grid values, e.g. padding: `${S[6]}px 0 ${S[5]}px` (32/24) or `${S[5]}px 0` — every padding must come from S.
- **[LOW] SPACING GRID (off-grid padding)** (line 185)  
  The trust-tier pill (line 185) and the identical 'New Seller' pill (line 199) use padding '5px 12px'; the vertical 5px is off the S grid (S[1]=4, S[2]=8).  
  _Fix:_ Use grid values, e.g. padding: `${S[1]}px ${S[3]}px` (4/12) on both lines 185 and 199 — or render these small status chips as badge('default'), which already carries on-grid padding.

## `src/app/item/[id]/page.tsx`  (3M 2L)

- **[MED] TOKENS ONLY (rule 1) — hardcoded shadow on glass** (line 239)  
  The sticky header is a top-level glass surface but hand-rolls its shadow with a hardcoded rgba(0,0,0) instead of the theme token. It won't adapt to dark mode and duplicates glass chrome the helper already supplies via var(--glass-shadow).  
  _Fix:_ Build the header from glass()/card() (boxShadow: 'var(--glass-shadow), var(--glass-inner)'), or at minimum replace the literal with boxShadow: 'var(--glass-shadow)'.
- **[MED] TYPE SCALE (rule 2)** (line 472)  
  The admin 'Mark Authenticated' button overrides btn()'s fontSize (14) with a raw off-scale 12. Raw fontSize outside t() is disallowed, and overriding the button helper's size to a non-scale value breaks the one-button-system rule.  
  _Fix:_ Drop the fontSize:12 override and keep btn()'s size; if a smaller control is truly needed source it from t('meta').
- **[MED] TYPE SCALE (rule 2)** (line 480)  
  The admin 'Flag' button overrides btn()'s fontSize with a raw off-scale 12, same as its sibling.  
  _Fix:_ Remove the fontSize:12 override so the button keeps the system size; if smaller text is required, source it from t('meta').
- **[LOW] TYPE SCALE (rule 2) / SPACING GRID (rule 3) / radius token** (line 564)  
  The 'Current' owner pill uses a raw off-scale fontSize:10 (below the scale's smallest 11), raw borderRadius:999 instead of the --pill token, and off-grid padding '2px 8px' (2 is not on S). The #15121C / white-overlay colors are the intentional fixed-light Tall  
  _Fix:_ Use ...t('micro') for the label, borderRadius: 'var(--pill)', and S-grid padding (e.g. `${S[1]}px ${S[2]}px`).
- **[LOW] TYPE SCALE (rule 2) / SPACING GRID (rule 3)** (line 566)  
  The wallet/monospace line uses a raw fontSize:11 (should come from the scale, not a literal) and an off-grid marginTop:2 (2 is not a value in S).  
  _Fix:_ Spread ...t('micro') (which is 11px) for the size and use marginTop: S[1] (4) for grid-aligned spacing.

## `src/components/kyc-verify.tsx`  (3M 1L)

- **[MED] TYPE SCALE (raw fontSize off-scale)** (line 110)  
  The Personal/Business segmented buttons override the button with a raw fontSize: 13, off the type scale (btn() already sets 14).  
  _Fix:_ Remove fontSize:13 (rely on btn()'s 14). If a two-option toggle is wanted, use tabSlider(), the canonical segmented control.
- **[MED] SPACING GRID (off-grid padding)** (line 72)  
  The 'Identity verified' card uses pad: '14px 16px' — 14px is off the 4/8 grid.  
  _Fix:_ Use card({ pad: S[4] }) (16) or '12px 16px' (S[3]/S[4]) to stay on-grid.
- **[MED] SPACING GRID (off-grid padding)** (line 110)  
  The Personal/Business buttons use padding: '9px 12px' — 9px is off the 4/8 grid.  
  _Fix:_ Use padding: '8px 12px' (S[2]/S[3]) or drop the override and use btn()'s default padding.
- **[LOW] SPACING GRID (off-grid margin)** (line 77)  
  marginTop: 1 is an off-grid 1px nudge under the 'Identity verified' label.  
  _Fix:_ Remove the 1px nudge or use gap: S[1] (4) on the parent flex column instead of a hand-tuned margin.

## `src/components/listing-card.tsx`  (3M )

- **[MED] 1 — Tokens only (no hardcoded theme colors)** (line 47)  
  card() supplies the mode-aware 'var(--glass-shadow), var(--glass-inner)' (light rgba(30,30,34,.14) / dark rgba(0,0,0,.55)), but this overrides it with a hardcoded rgba(15,15,30,.x) stacked shadow. The literal theme color won't flip with light/dark, and the hea  
  _Fix:_ Drop the boxShadow override and let card() supply 'var(--glass-shadow), var(--glass-inner)'. If more lift is needed, add a token (e.g. --glass-shadow-raised) in tokens.css and reference it — never a literal rgba theme color.
- **[MED] 3 — Spacing grid (off-grid padding + negative overlap)** (line 45)  
  The info-box padding '12px 13px' has an off-grid 13 (grid has 12/16, not 13) and the -14px overlap margin is off-grid (grid has 12/16). Duplicated verbatim in pending-serial-card.tsx (margin line 44, padding line 45).  
  _Fix:_ Snap to the grid: padding `${S[3]}px ${S[4]}px` or `${S[3]}px ${S[3]}px`, and use a grid-based overlap (-S[3] = -12 or -S[4] = -16). Since both cards share this exact block, factor it into one shared style.
- **[MED] TOKENS ONLY (hardcoded theme shadow) + skeuomorphic multi-shadow** (line 47)  
  The overlapping info box (rendered on this route via ListingCard) overrides card()'s token shadow with two hand-rolled near-black rgba shadows. They don't flip with the theme and are the kind of skeuomorphic multi-shadow the glass system explicitly avoids.  
  _Fix:_ Drop the override and let card() supply boxShadow: 'var(--glass-shadow), var(--glass-inner)'. If a stronger lift is needed for the photo overlap, add a token (e.g. --card-lift) in tokens.css and reference it, rather than literal rgba(15,15,

## `src/app/page.tsx`  (2M 3L)

- **[MED] 2 — Type scale (raw fontSize off-scale) + emoji/glyph icon** (line 252)  
  The clear-search button uses a raw fontSize:18 (off the t() scale) on a literal '×' glyph. It is both an off-scale font size and a non-SVG text glyph used as an icon (the codebase already renders an SVG X cross at page.tsx line 273).  
  _Fix:_ Replace the '×' text glyph with an inline SVG X icon (a small line-cross, as already used at page.tsx line 273) so there's no off-scale fontSize and no text-glyph icon.
- **[MED] 3 — Spacing grid (off-grid padding)** (line 265)  
  The Filters chip overrides btn()'s padding with '6px 13px' — neither 6 nor 13 is on the S grid (4/8/12/16/24/32/48/64). The same off-grid '6px 13px' / '6px 14px' pattern repeats on the chip buttons at lines 271, 304, 318, 329, 340, 369.  
  _Fix:_ Use grid values — e.g. padding: `${S[2]}px ${S[3]}px` (8/12) — or add a small pill-chip size to btn() in ui.ts and use it consistently, rather than re-deriving off-grid padding at every call site.
- **[LOW] 1 — Tokens only (no hardcoded theme colors)** (line 218)  
  The sticky top-nav glass surface hardcodes rgba(0,0,0,.08) instead of the token. A pure-black shadow disappears into the dark field in dark mode, so it won't adapt, and it bypasses the glass shadow system that var(--glass-shadow) provides.  
  _Fix:_ Reference 'var(--glass-shadow)' directly, or build the nav from glass()/card() (which supply 'var(--glass-shadow), var(--glass-inner)'), instead of the literal rgba.
- **[LOW] 1 — Tokens only (modal scrim color)** (line 282)  
  The filters bottom-sheet backdrop hardcodes rgba(0,0,0,.5). A real --modal-scrim token exists (tokens.css: light rgba(15,12,24,.45) / dark rgba(0,0,0,.65)) and first-run-onboarding.tsx uses 'var(--modal-scrim, …)', so this raw value is an untokenized inconsist  
  _Fix:_ Use 'var(--modal-scrim)' (matching first-run-onboarding.tsx) so every modal backdrop shares one tokenized scrim.
- **[LOW] 5 — Radius scale (off-token radius)** (line 283)  
  The filters sheet uses a 30px top radius, off the radius scale (--r-xl is 28px, the modal/sheet token). The header-menu sheet (line 75) repeats the same 30px, while first-run-onboarding.tsx line 89 correctly uses 28px — so 30 is the odd one out.  
  _Fix:_ Use the token: radius: 'var(--r-xl) var(--r-xl) 0 0' (28px), matching the onboarding sheet, so all bottom sheets share one corner radius.

## `src/app/help/page.tsx`  (2M 2L)

- **[MED] TOKENS ONLY — hardcoded theme shadow** (line 155)  
  The sticky header is a top-level glass surface but hardcodes a literal rgba(0,0,0,.06) shadow instead of the theme shadow token. --glass-shadow differs by mode (light .14/.06, dark .55/.35), so this fixed .06 will not flip between light and dark and diverges f  
  _Fix:_ Use the glass shadow token: boxShadow: 'var(--glass-shadow)' (or compose the header from glass()/card() so the shadow comes from tokens).
- **[MED] SPACING GRID — off-grid margin** (line 205)  
  6px is not on the S spacing grid ({1:4,2:8,3:12,4:16,...}); the label-to-input gap is a hand-rolled off-grid value. Repeated on the Subject (218), Order ID (230), and Message (242) labels.  
  _Fix:_ Snap to the grid: marginBottom: S[2] (8) — apply to all four field labels on lines 205, 218, 230, 242.
- **[LOW] SPACING GRID — off-grid padding override** (line 157)  
  The back button spreads btn()'s grid padding ('12px 20px') then overrides it with off-grid '6px 10px' (6 and 10 are not on the S scale), defeating the canonical button padding.  
  _Fix:_ Drop the override and let btn() supply padding, or if a smaller icon button is needed use a grid-aligned value like padding: `${S[2]}px ${S[3]}px` ('8px 12px').
- **[LOW] SPACING GRID — off-grid padding** (line 165)  
  The bottom-nav clearance padding of 100px is an arbitrary off-grid value not drawn from the S scale ({...,7:48,8:64}).  
  _Fix:_ Use a grid-composed value for the clearance, e.g. paddingBottom: S[8] + S[6] (64+32) or another sum of S tokens rather than a raw 100.

## `src/components/pending-serial-card.tsx`  (2M )

- **[MED] 1 — Tokens only (no hardcoded theme colors)** (line 46)  
  Same as ListingCard: overrides card()'s var(--glass-shadow) with a hardcoded rgba(15,15,30,.x) stacked shadow — a theme color that won't flip with mode, plus a heavy multi-shadow that violates the 'diffuse/ambient only, glass never glows' rule.  
  _Fix:_ Remove the boxShadow override and inherit card()'s 'var(--glass-shadow), var(--glass-inner)', or introduce a shared raised-shadow token in tokens.css and reference it from both cards.
- **[MED] TOKENS ONLY (hardcoded theme shadow) + skeuomorphic multi-shadow** (line 46)  
  PendingSerialCard (the business storefront card on this route) has the same hand-rolled near-black rgba double-shadow overriding card()'s token shadow; won't flip with light/dark.  
  _Fix:_ Same as ListingCard — remove the override and rely on card()'s 'var(--glass-shadow), var(--glass-inner)', or introduce a shared shadow token for the overlapping info-box lift.

## `src/components/preset-composer.tsx`  (2M )

- **[MED] SPACING GRID** (line 66)  
  Confirmed at line 66. This chip overrides btn() with off-scale fontSize 13 (t() has 16/14/12/11 — design.md §9 explicitly lists `fontSize: 13` as a Don't) and off-grid padding '9px 14px' (9 and 14 are not on S {4,8,12,16,24,32,48,64}). The chip is reused for N  
  _Fix:_ Use btn('secondary') as-is (it ships fontSize 14 and padding '12px 20px'), or for a smaller pill standardize on `...t('meta')` with an on-grid pad like `padding: S[3]` (12) or '8px 12px'.
- **[MED] TYPE SCALE** (line 109)  
  Confirmed at line 109. Raw fontSize 13 is off the type scale and vertical padding 9 is off the S grid. The same fontSize-13 override recurs at L81 and L102 (btn('text')). (L82 uses fontSize 14, which is on-scale = body, so that spot is fine.)  
  _Fix:_ Remove the fontSize override (btn already ships fontSize 14); if a smaller label is required apply `...t('meta')`. Use an on-grid pad such as `padding: S[3]`.

## `src/app/liked/page.tsx`  (1M 1L)

- **[MED] 1 — Tokens only (hardcoded theme color)** (line 35)  
  The sticky header shadow hardcodes rgba(0,0,0,.06), a theme value that does not flip with light/dark. tokens.css defines a theme-aware --glass-shadow (light 0 12px 40px rgba(30,30,34,.14)... / dark 0 18px 50px rgba(0,0,0,.55)...). At .06 opacity this shadow is  
  _Fix:_ Use the theme token: boxShadow: 'var(--glass-shadow)'. Since this sticky bar is top-level floating chrome, spreading glass() (which already applies var(--glass-shadow), var(--glass-inner)) would be cleaner than hand-rolling the background/b
- **[LOW] 2 — Type scale + component sizing (avatar)** (line 75)  
  This spreads avatar('sm') (32px, fontSize 12) then overrides it to width/height 22 and fontSize 10. 22 is not one of the avatar sizes (sm 32 / md 48 / lg 64) and fontSize 10 falls below the type scale's smallest role (micro = 11), so the initial glyph is set w  
  _Fix:_ Use avatar('sm') as-is (32px, its built-in fontSize 12) for the seller avatar. If a smaller mark is genuinely needed, add a real size to the avatar() size map rather than overriding width/height/fontSize inline, and keep the label glyph on 

## `src/components/tally-explainer.tsx`  (1M )

- **[MED] TYPE SCALE (rule 2) / SPACING GRID (rule 3) / radius token** (line 12)  
  The 'NFT' chip uses a raw off-scale fontSize:9 (below micro/11), a raw borderRadius:999 instead of var(--pill), and off-grid padding '2px 7px' (neither value is on S). The color literals are the intentional fixed-light Tally surface, but the type/spacing/radiu  
  _Fix:_ Render as badge() or at minimum use ...t('micro'), borderRadius: 'var(--pill)', and S-grid padding like `${S[1]}px ${S[2]}px`.

## `src/components/payout-settings.tsx`  (1M )

- **[MED] NOTHING GLOWS except the dark-mode background (design.md §2) — coloured box-shadow-as-glow on nested surface()** (line 177)  
  Confirmed lines 177 and 215: selected payout-type / asset tiles apply a coloured blue-ish box-shadow rgba(90,160,210,.22) as a selection glow on surface() panels. §2: nothing glows except the dark-mode background field, and glass never glows. Selection is alre  
  _Fix:_ Drop the coloured glow; keep var(--box-shadow-soft) and rely on the existing gradient check badge / gradient-clipped label to indicate selection.

## `src/components/dispute-panel.tsx`  (1M )

- **[MED] GLASS-NESTING LAW (glass tone on nested chip)** (line 187)  
  Verified: this non-image evidence icon box fills with var(--glass-bg) (a glass tone). It sits inside the surface() evidence row (the <a> at line 178), which is inside the DisputePanel card() (line 354, strong glass) that renders on /order/[itemId]. ui.ts (line  
  _Fix:_ Replace the hand-rolled background: 'var(--glass-bg)' with the surface() helper, e.g. spread ...surface({ radius: 'var(--r-sm)' }) onto the span (matching header-menu.tsx lines 81/92) so the nested chip is a near-solid inset panel instead o

## `src/app/dashboard/page.tsx`  (2L)

- **[LOW] SPACING GRID** (line 736)  
  Confirmed at line 736. Revenue stat tiles use pad '14px 12px'; vertical 14 is off the S grid (12 is on-grid).  
  _Fix:_ Use an on-grid pad — `surface({ pad: S[3] })` (12) or `surface({ pad: '16px 12px' })` (S[4]/S[3]).
- **[LOW] SPACING GRID** (line 895)  
  Confirmed at line 895. The Pay / Request button in the thread header overrides btn() padding to '8px 14px'; horizontal 14 is off the S grid. btn() already supplies on-grid padding '12px 20px'.  
  _Fix:_ Drop the padding override (use btn('secondary') as-is) or use an on-grid value like '8px 12px' (S[2]/S[3]).

## `src/components/tally-card.tsx`  (2L)

- **[LOW] GRADIENT BUDGET + TOKENS ONLY (gradient fill + hardcoded shadow on a content card)** (line 27)  
  TallyCard (rendered on the My Tallys tab, page.tsx line 134) paints a full gradient across the card body — a content surface that is NOT among design.md's allowed gradient targets (CTA / price / active tab / avatar / story ring / heading). The drop shadow rgba  
  _Fix:_ If the gradient Tally object is intended, document it in design.md/ui.ts as an approved gradient surface (like avatar/story ring). At minimum move the drop shadow to a token (--box-shadow-soft / --glass-shadow) and put padding on-grid (12/1
- **[LOW] TYPE SCALE (raw off-scale fontSizes in imported component)** (line 34)  
  TallyCard renders on the profile My Tallys tab and uses raw off-scale font sizes (15, 8.5, 10.5) and hand-set weights; only 11 matches a role. Off-scale sizes are disallowed except price().  
  _Fix:_ Map to scale roles: title -> t('heading') or t('body'); serial -> t('meta'); owner count -> t('meta'); the TALLY eyebrow -> t('micro') or badge(). Keep INK/INK_DIM as the deliberate fixed ink on the always-light tally gradient (acceptable, 

## `src/components/like-button.tsx`  (1L)

- **[LOW] 2 — Type scale (raw fontSize off-scale)** (line 57)  
  The like count (rendered on every home-grid ListingCard via variant='bare') hand-writes fontSize:12/fontWeight:700 rather than a scale role. 12/700 matches no single role (meta is 12/500, micro is 11/700), so it's a bespoke size off the t() scale.  
  _Fix:_ Use a scale role, e.g. {...t('meta')} for the count and let color override, instead of hand-writing fontSize/fontWeight.

## `src/components/owner-stack.tsx`  (1L)

- **[LOW] 3 — Spacing grid (off-scale avatar size)** (line 35)  
  OwnerStack defaults avatars to size 26 (and AvatarCircle defaults to 28) — neither is on the avatar() scale (sm 32 / md 48 / lg 64). The home-grid ListingCard calls OwnerStack with no size, so 26 renders. Bespoke avatar sizing outside the avatar() helper.  
  _Fix:_ Drive avatar diameters from the avatar() scale (sm 32) or add an explicit small token size; avoid arbitrary 26/28 diameters so avatars stay consistent across the app.

## `src/components/shipping-estimator.tsx`  (1L)

- **[LOW] SPACING GRID (off-grid padding)** (line 113)  
  The payout-readout surface uses pad: '12px 14px' — the 14px horizontal is off the 4/8 grid.  
  _Fix:_ Use pad: '12px 16px' (S[3]/S[4]) to land on the grid.

## `src/app/wallet/page.tsx`  (1L)

- **[LOW] TOKENS ONLY (design.md §6) — hardcoded rgba(0,0,0) theme colour on a glass surface** (line 56)  
  Confirmed line 56: the sticky glass header hardcodes a black theme shadow rgba(0,0,0,.08) instead of the system's var(--glass-shadow). Black-rgba is a theme colour that won't tune between light and dark. (Note: this same literal is repeated on the homepage hea  
  _Fix:_ Use boxShadow: 'var(--glass-shadow)' (or the glass() helper) instead of the literal rgba(0,0,0,.08), and align the homepage header to match.

